# نشر 24-1 — patch-2026.08.24-1

> **رقعةٌ صغيرةٌ فوق 22-1** (ملفّان فقط: `ldap_auth.py` + `BUILD.json`) — تحتاج أن تكون 22-1 مطبَّقةً قبلها.
> بصمة الرقعة **داخل** التعمية: `94c377fa535dcbef145a84d6` — قارنها بعد الفكّ قبل التطبيق.
> **لا تمسّ البيانات ولا المصادقة**: `data/` و`refs/` و`outputs/` و`evidence/` لا تُلمس، وحقولُ POST كما هي.

## ما فيها — صفحةُ الدخول الجديدة (اعتمدها المالك 24/8)

بطاقةُ الدخول الوسطى استُبدلت بـ**شاشةٍ مقسومة**:

1. **النصفُ البصريّ**: طبقاتُ المعمار الخمسُ حيّةً على `<canvas>` — الأعمالُ · التطبيقاتُ · البياناتُ · التقنيةُ · الأمن — عقدُها موصولةٌ ونبضٌ ينزل بينها طبقةً طبقة، مع عنوانٍ يشرح ما المستودعُ في سطر.
2. **النصفُ النظيف**: النموذجُ بحقوله كما هي — الرقمُ الوظيفيّ (`ident`) وكلمةُ حساب الشبكة (`password`) → `POST /login`. **آليّةُ المصادقة لم تُمسّ إطلاقًا.**
3. **بلا اتّصالٍ خارجيّ**: الخطُّ والشعارُ يخرجان من `brand.py` مضمّنَين — قِيس بالمتصفّح: **صفر طلباتٍ خارجيّة**.
4. **ثلاثُ حالاتِ ثيم** (فاتحٌ · نظامٌ داكن · اختيارٌ صريح) وتجاوبٌ كامل: على الجوّال ينقلب الانقسامُ إلى عمودٍ واحدٍ **بلا تمريرٍ أفقيّ**.
5. **تحقّقٌ عربيٌّ قبل الإرسال** (الرقمُ الوظيفيّ أرقامٌ فقط) وحالةُ تحميلٍ على الزرّ؛ ورسالةُ خطأ الخادم تظهر في مكانها كما كانت.

**الفحص**: وُلِّدت الصفحةُ من كود المنصّة نفسه (الحالةُ العاديّةُ وحالةُ الخطأ) وفُتحت في Chromium على 1440×900 و390×844 — صفر أخطاء JS، صفر طلباتٍ خارجيّة، صفر تمريرٍ أفقيّ، والخطُّ المضمّنُ مطبَّقٌ فعلًا. وصياغةُ بايثون مفحوصةٌ بـ`ast`.

## التطبيق (المعتاد)

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.24-1.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.24-1.tar.gz | cut -c1-24    # يجب 94c377fa535dcbef145a84d6
./patch.sh patch-2026.08.24-1.tar.gz
```
وللتراجع: `./patch.sh --rollback`

## بعد التطبيق — تحقّقان

1. **الصفحة**: افتح `/login` — يجب أن ترى الشاشةَ المقسومة والرسمَ يتحرّك، وشعارَ المدينة أعلى اليمين.
2. **الدخول نفسه**: ادخلْ برقمٍ وظيفيٍّ صحيح — يجب أن يعمل كما كان؛ وبرقمٍ خاطئ يجب أن تظهر رسالةُ الخطأ الحمراءُ داخل الصفحة الجديدة.

⚠️ **إن ظهرت الصفحةُ بلا خطّ المدينة** (خطٌّ نظاميٌّ بدلًا منه) فذلك يعني أنّ `assets/` ناقصةٌ على الخادم — الصفحةُ تعمل، لكن أبلغْ لتُشحن الأصول.
