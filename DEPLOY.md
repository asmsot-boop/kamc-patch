# رقعة 2026.08.15-5

> 🔐 `-pass file:$HOME/.config/kamc/deploy_pass` · ⛔ **بلا `sudo`** — والسكربت يرفضه من نفسه.

**ستّة أبواب — كلُّها من ملاحظاتك اليوم، وكلُّ سببٍ مقيسٌ لا مُخمَّن.**

---

## ① الخروج وأنت نشط — والتشخيصُ السابق كان يتّهم بريئًا

*«أكون في صفحة وأطلب أخرى وأنا نشط — يخرجني ويطلب تسجيل الدخول.»*

**ليس `nginx auth_basic`** كما ظننّا شهرًا (البند ١٥ في السجلّ). السببُ أنّ
`session.EXEMPT` كانت **قائمةً واحدةً لغرضين**:

- «لا تُظهر صفحةَ قفلٍ هنا» — صحيحٌ للتنزيلات، فصفحةُ قفلٍ مكان PDF تفسده.
- «لا تعدَّ هذا نشاطًا» — **وهذا هو الخطأ.**

وفيها `/repo/doc/` — **ومحرّرُ الوثائق كلُّه تحته**. فتعمل في المحرّر
عشرين دقيقة **والطابعُ مجمَّدٌ طوالها**، ثمّ تنقر شاشةً أخرى فيراك
ساكنًا عشرين دقيقة فيقفل. **وسجلُّ القفل يشهد**: `سكون 15 · 16 · 20 · 21`
— أرقامٌ تلي الحدَّ مباشرةً، لا سكونًا حقيقيًّا.

فُصلت إلى `NO_LOCK` و`NO_TOUCH`. **والتحريرُ والتنزيلُ عملٌ لا سكون،
ومن عمل لا يُعامَل معاملةَ الغائب.** ولا يجدّد إلّا `/logout` و`/locked`.

## ② الخزنة تُعدَّل — وكان التعديلُ يمحو

*«إذا أضفت بيانات اتصال لا أستطيع التعديل لو فشل.»* **والعطبُ أكبر:**

```
قبل:  host=10.0.0.9 · port=443 · user=svc-ea · extra=ملاحظة
وبعد تعديل المنفذ وحدَه:
      host=''       · port=8443 · user=''    · extra=''
```

**والكلمةُ وحدَها كانت محروسة** — والتعليقُ فوق الدالّة يحذّر من هذا
بعينه ثمّ يحرس حقلًا واحدًا. **وحارسُ نصفِ الحقول يطمئن ولا يحمي.**

الآن: الفارغُ يبقي القديمَ في **كلّ** حقل · **✏️ تعديل** يفتح النموذجَ
ممتلئًا **والاسمُ مقفلٌ** فلا تخلق سجلًّا ثانيًا · **🗑 حذف** بتأكيد ·
**والكلمةُ لا تُعرض أبدًا** ولا في وضع التعديل.

## ③ ألوان قوالب الإكسل

*«ألوانها مزعجة وغير متناسقة»* — والسببُ أنّ ثلاثةَ ألوانٍ داكنةٍ مشبعةٍ
صُمّمت لواجهةٍ ليليّة (بنّيّ `5A3A1C` · ذهبيّ `8A6D00` · كحليّ) وُضعت على
**ورقةٍ بيضاء**. والأسوأ أنّ البنّيّ يُصبغ على **كلّ خليّةٍ فارغةٍ في كلّ
عمود** — **ووسمٌ يعمّ كلَّ شيءٍ لا يشير إلى شيء.**

```
كان:  1500 خليّة بنّيّة داكنة
صار:   300 خليّة كهرمانيّة فاتحة — على الحقول المطلوبة وحدَها (خفض 80٪)
```

والترويسة: **أزرقان من أسرةٍ واحدة** (كحليّ · أزرق الهويّة) بدل كحليٍّ
وذهبيٍّ متنافرين.

## ④ قارئ الوثائق يعلن تعطّله

*«أختار «اقرأ الوثيقة الآن» ولا ردّة فعل.»* — **والزرُّ معطَّلٌ فعلًا**
(`disabled`) لغياب مفتاح النموذج، **ولا رسالةَ تقول لماذا**. **وصمتُ
الواجهة يحوّل قرارًا إلى عطبٍ في ذهن من يقرأها.**

صار فوقه تنبيهٌ يميّز الحالتين: **أُطفئ يدويًّا** (وفيه رابطُ إعادته) أم
**لا مفتاحَ على هذا الخادم** (وفيه الطريقُ البديل: الاستخراجُ اليدويّ).

## ⑤ الوثائق: اسمٌ وتاريخُ طباعةٍ واعتمادٌ وحدَه

- **اسمُ رئيس القسم** في الترويسة والتوقيع: `م. عبدالله بن سفر العتيبي`.
  وكان مكتوبًا في `pdf_out.SIGNERS` منذ 6/8 و`documents.py` **لا تعرفه** —
  فوُصلا بمصدرٍ واحد.
- **«تاريخ الطباعة»** بدل «تاريخ الإصدار» — والوثيقةُ المولَّدةُ تُبنى
  لحظةَ الطلب، فلا معنى لتاريخين.
- **جدولُ الاعتماد عمودًا واحدًا** بدل ثلاثةٍ (أعدّه · راجعه · اعتمده)
  لشخصٍ واحد. ⚠️ **وثلاثةُ أعمدةٍ لشخصٍ واحدٍ تصنع مسرحَ حوكمة**: خانةُ
  «راجعه» تبقى فارغةً في كلّ وثيقة **فيقرأ المفتّشُ أنّ المراجعة لم تقع**
  — وهي لم تكن مطلوبةً أصلًا. **وفراغٌ لا يُفسَّر أسوأ من عمودٍ لا يوجد.**

## ⑥ إسنادُ المدراء بالبحث لا بالرقم

*«لا أعرف أرقامهم الوظيفية»* — **ورقمٌ لا يعرفه أحدٌ حاجزٌ لا مدخل.**

الحقلُ صار حرًّا: **جزءٌ من الاسم أو البريد أو الرقم**، ويردّ **قائمةَ
مرشّحين** فيها اسمُ كلٍّ وبريدُه **وإدارتُه ومسمّاه** — فعشراتٌ في المدينة
يشتركون في الاسم الأوّل، **واختيارُ الأوّل تلقائيًّا يُسند «عبدالله»
الخطأ**. ومرشّحٌ واحدٌ يمرّ مباشرةً بلا سؤال.

🔒 وبُنيت `search_people` **دالّةً مستقلّةً ولم تُمسّ `lookup`** — لأنّ تلك
يمرّ بها **الدخولُ نفسُه**، ومطابقةٌ جزئيّةٌ هناك تعني أنّ «أحمد» يطابق
عشرةً فيدخل أوّلُهم. **والقراءةُ تتساهل، والمصادقةُ لا تتساهل أبدًا.**

---

## الحراسة

**ع٦٩** الخزنة · **ع٧٠** البحث والتوقيع · **ع٧١** النشاط واللون والتعطيل
المعلن — ولكلٍّ منها تأكيدٌ على السبب لا على العَرَض.

**الحزمة: ٧٨٧ نجح · ١ فشل.** والأحمرُ الوحيد: تسرّبُ نصٍّ عربيٍّ في
العرض الإنجليزيّ — **أكثرُه قيَمُ بيانات لا نصُّ واجهة**، وهو في السجلّ
بندًا مستقلًّا.

## التطبيق

```bash
cd ~/kamc_ea
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch.tar.gz \
  -pass file:$HOME/.config/kamc/deploy_pass
sha256sum -c <(echo "$(cat latest.sha256)  patch.tar.gz")
./patch.sh patch.tar.gz          # ⛔ بلا sudo
```

**وبعدها قِس لا تصدّق الرسالة**: `/repo/diag` يقول **2026.08.15-5** ·
`/repo/vault` فيه **✏️** و**🗑** في كلّ صفّ · `/repo/tpl` نزّل قالبًا
وانظر ألوانه · `/repo/directors` ابحث باسمٍ لا برقم · **واشتغل في محرّر
وثيقةٍ ربع ساعةٍ ثمّ انتقل لشاشةٍ أخرى — لا يجب أن يقفل.**
