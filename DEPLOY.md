# رقعة 2026.08.15-9 — **ملف اللغة يمتلئ من نفسه · وسبب الخروج يقيد**

> 🔐 `-pass file:$HOME/.config/kamc/deploy_pass` · ⛔ **بلا `sudo`**.

---

## ① 🔴 جذر يوم كامل — سطران في `_is_ui_term`

المالك 15/8: *«انت تروح للصفحات وتدور عليها واحدة واحدة — هذا أكبر غلط.
المفروض ما يقرأ إلا من عمود اللغة الإنجليزية، وكلمة ما لها ترجمة يطلعها
في ملف الترجمة. أنا أشيك على ملف الترجمة لا على الصفحات.»*

**وهو محق، والعلة كانت هنا:**

```python
if len(t) > 90:        return False   # جملة أطول من ٩٠ حرفا → ترفض
if t.count(" ") > 10:  return False   # أكثر من ١٠ كلمات → ترفض
```

⚠️ **وهذه بالضبط الجمل الوصفية التي يراها المالك عربية**. فترفض صامتة،
**فلا تدخل ملف اللغة ولا تظهر فراغا ينتظر الملء** — فيراها هو على
الشاشة، ونطاردها نحن صفحة صفحة. **وملتقط يرفض ما لا يفهمه يترك المالك
يفتش بعينه.**

🔑 **والقاعدة الصحيحة**: ما وصل إلى الملتقط **قد عرض فعلا في صفحة**
(`apply` لا ترى إلا العقد المعروضة)، **فتعليقات المطور لا تصل أصلا** —
والفلتر كان يحمي من شيء محمي منه. صار الحد **٦٠٠ حرفا**، ويبقى استبعاد
الشفرة والروابط وحدها.

**والدورة الآن كما طلب المالك:**

```
نص عربي يظهر في العرض الإنجليزي
  → يلتقط تلقائيا وقت العرض
  → يدخل ملف اللغة فراغا  (كنس كل ٦ ساعات، وبعد ٥ دقائق من الإقلاع)
  → يظهر في /repo/i18n بحقل ملء
  → يملؤه المالك
```

**فلا يفتح إلا `/repo/i18n`.** قيس بعد الإصلاح: **٥ نصوص دخلت الملف
فراغات وظهرت كلها في الشاشة** — وكانت مرفوضة صامتة قبل دقيقة.

⚠️ **وحد واحد يبقى**: النص **المولد** مركبا (اسم عنصر + رقم) لا مفتاح
ثابت له، **فيصلح في مصدره لا بملء ملف**. وهو قليل ومعروف.

## ② 🕵️ سبب الخروج يقيد لحظة وقوعه

المالك: *«مشكلة الخروج المتكررة لا زالت»* — **بعد إصلاح `EXEMPT` في
`-5`**. فالعلة غيره أو معه.

⚠️ **وثلاث علل تخرج المستخدم ولا تفرق بينها رسالة واحدة**، ولكل واحدة
علاج مختلف تماما. **ومن لا يميزها يصلح غير العلة** — وقد أصلحنا واحدة
والمالك يعاني من أخرى.

| القيد الجديد | ما يعني |
|---|---|
| `SESSION_LOCK · السبب: سكون` | جلسة شاخت داخل المنصة (الحد ١٥ دقيقة) |
| `SESSION_LOCK · السبب: توقيع لا يطابق` | تبدل المفتاح أو البريد — **ليس سكونا، ولا يعالج بتمديد المهلة** |
| `IDENTITY_LOST` | **وصل الطلب بلا هوية أصلا** — ومعه الترويسات التي وصلت، فيعرف أيها سقط |

ويقيد معها **المسار والدقيقة والحد**. فأول مرة يخرج فيها المالك:
**افتح `/repo/errors` — السبب مكتوب.** **ولا تخمين رابعا.**

---

**الحزمة: ٨٣٦ نجح · صفر فشل.**

## التطبيق

```bash
cd ~/kamc_ea
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch.tar.gz \
  -pass file:$HOME/.config/kamc/deploy_pass
sha256sum -c <(echo "$(cat latest.sha256)  patch.tar.gz")
./patch.sh patch.tar.gz          # ⛔ بلا sudo
```

**وبعدها**: `/repo/diag` → **2026.08.15-9** · افتح **`/repo/i18n`** وانظر
عدد الفراغات — **هذا هو المكان الوحيد الذي تراجعه**. وأول خروج: **`/repo/errors`**.
