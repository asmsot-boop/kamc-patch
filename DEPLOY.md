# نشر 16-9 — patch-2026.08.16-9

> **تجُبّ كل ما قبلها** (لقطة جذر كاملة · 170 ملفا · النسخة 2026.08.16-9) — المدينة على 16-8.
> بصمة الرقعة داخل التعمية: `3b98fe5517ca0a0f90e4d74f` — قارنها بعد الفك قبل التطبيق.
> `shared_viz.py.enc` لم يتغير منذ 16-4 — لا خطوة يدوية.

## ما فيها — دفعة مساء 16/8

1. **🚪 جذر «الخروج غير المبرر» (الأهم)**: هوية دخول AD كانت تصدر عند الدخول فقط وتموت بعد 15 دقيقة **من الدخول لا من آخر نشاط** — فتطرد النشط (وسجلكم أكده: 741 قيد IDENTITY_LOST). الآن تتجدد مع كل نشاط كالجلسة — العد من آخر نقرة.
2. **⭐ أيقونة التبويب — الجذر الذي شخصه فحصكم**: بوابة `_actor_mw` القديمة حصرت بمسارات `/repo`/`/api` — الأصول العامة تفتح بلا ارتباط (تحقق: `/assets/favicon.png` بتصفح خفي = 200).
3. **📜 شاشة السجلات**: فلتر باليوم (تقويم) + بالمستخدم (قائمة) + بحث، و**25 سطرا** بعداد «1–25 من N» وتالي/سابق — بدل 300 دفعة. والطوابع الزمنية إنسانية بتوقيت مكة («اليوم 19:45 · أمس · 12/08») بدل ISO غرينتش — في كل شاشات السجلات.
4. **✍️ حفظ المصطلح في محرر الترجمة صُلب**: مطابقة مطبعة للمفاتيح متعددة الأسطر (كانت تفشل بصمت) + أي فشل حفظ يقيد في سجل الأخطاء ويسمي المصطلح.
5. **ضجيج السجل**: IDENTITY_LOST لا يقيد على /login و/logout (بلا هوية بطبيعتهما).
6. **🧩 أيقونات الرف المضموم**: مجموعتا الهندسة المدموجتان أضيفتا لخريطة المكتبة الموحدة — كل المجموعات Lucide أحادية اللون، لا إيموجي ملونا.

### 8. ثلاثية بلاغ الليل (16-9)
- **13 أيقونة بند داخلية** أضيفت لخريطة Lucide — الرف كله أحادي اللون الآن فعلا.
- **توقيت سجل الأثر**: كان يعرض +3 (اليوم 23:20 والساعة 20:20) — الساذج يفسر مكيا.
- **فلتر المستخدمين في السجلات** يبنى من القاعدة كلها — كل الموظفين يظهرون لا أحدث 300 صف.

### 9. 📖 بذر القارئ إن منعه المخزن القديم
البذرة لا تدهس مخزنا قائما (تصميم). إن ظهر «تخطت» كما في فحصكم: **الحذف الآمن الذي جهزتموه معتمد** (النسخة مأخوذة والقديم مشمول في البذرة وصفر مقبول) — نفذوه ثم أعيدوا التشغيل وتحققوا من «📖 بذرت 13 وثيقة».

## التطبيق (المعتاد)

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.16-9.tar.gz -pass pass:<الكلمة المعتادة>
./patch.sh patch-2026.08.16-9.tar.gz
```

## بعد التطبيق — أربعة تحققات

1. **الخروج**: ادخل واعمل 20+ دقيقة بنشاط متقطع — لا طرد. (والسكون الحقيقي 15 دقيقة يقفل كما صمم.)
2. تصفح خفي: `/assets/favicon.png` → 200، والنجمة بتبويب جديد.
3. `/repo/logs`: الفلاتر الثلاثة + 25 سطرا بالعداد.
4. الرف المضموم (زر الضم): كل أيقونات المجموعات أحادية اللون.

### 7. 📖 **تفعيل قارئ الوثائق (المرحلة 1)** — الإضافة الكبرى في 16-8
بذرة 314 مقترحا بشواهدها الحرفية من 13 وثيقة مؤسسية (إطار KEA · السياسات · تحليل الفجوة · الموائمة · قياس V5.0…) استخرجت وروجعت في الورشة — **بلا مفتاح نموذج في المدينة وبلا إخراج أي شيء منها**. أول إقلاع يبذرها مرة (سترى «📖 بذرت 13 وثيقة») ثم: **الإدخال والمخرجات ← قارئ الوثائق ← راجع كل مقترح بشاهده واقبل واحدا واحدا** — المقبول يوسم «مستخرج آليا — مراجع» بمصدره. هذا يملأ طبقة الأعمال الفارغة التي تخفض المؤشر. (4 وثائق تعذر استخراجها وتبقى لمعالجة لاحقة، والوثيقة الداخلية لم تمس.)

للتراجع: `./patch.sh --rollback`.
