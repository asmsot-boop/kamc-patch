# رقعة 2026.08.15-6 — **الحزمة كلها خضراء لأول مرة**

> 🔐 `-pass file:$HOME/.config/kamc/deploy_pass` · ⛔ **بلا `sudo`**.

**٨٢٠ نجح · صفر فشل.** والأحمر الذي حملناه أياما تبين أنه **عطب في
المقياس لا في المنصة** — تفصيلُه في ⑦ أدناه.

---

## ① جمود الخمس ثوان — وثقوب سجل الأثر

`versions.stamp` كان يستغرق **5.019ث في كل حفظ وثيقة**. والسبب:
`repo._trail_echo` يفتح اتصالا **ثانيا على القاعدة نفسها** (`trail.db`
هو `repo.db`) **وهو داخل معاملة كتابة مفتوحة** — فينتظر قفلا لا يفرج
إلا بعد `commit` لا يقع حتى يعود هو. **جمود ذاتي مدته المهلة بالضبط.**

⚠️ **وWAL لا يحل هذا**: يفصل القارئ عن الكاتب، **والاثنان هنا كاتبان**.
ورفع المهلة **يضاعف الانتظار ولا ينجح** (قيس: 10.01ث ثم فشل).

🩸 **والأخطر أن القيد كان يضيع**: يرمي «database is locked» فيبتلعه
`except: pass` — **فسجل الأثر فيه ثقوب صامتة**، وهو سجل مساءلة.
**والثقب في سجل الأثر عيب حوكمة لا عيب أداء.**

**العلاج**: الصدى يكتب **بالاتصال نفسه**، فلا كاتبان على قاعدة واحدة.
وأي فشل **يقيد في سجل الأخطاء ولا يبتلع**.

```
قياس:  5.019ث → 0.004ث   ·   والقيد يكتب فعلا
```

## ② مخرج Word — ثلاثة عيوب لا واحد

- **مربعات □**: الخط العربي لا يحمل `⟨⟩` ولا الرموز، **فيرسم Word مربع
  «محرف مفقود»** والقارئ يظنها عطب ملف. → `⟨يملأ⟩` صارت `(يملأ)`.
- **الترقيم ينقلب**: `/repo/reg/principle.` تعرض `.repo/reg/principle/`
  لأن المقطع اللاتيني داخل جرية عربية **يرث اتجاهها**. → يفصل في جرية
  خاصة بلا `w:rtl` — **والاتجاه خاصية جرية لا خاصية فقرة**.
- **وخلايا الجداول تمر بالتنقية كذلك** — وكانت تكتب خامة، **وجدول
  الاعتماد أظهر ما في الوثيقة**.

## ③ 🕵️ الرقيب الذكي — `/repo/watchman`

**شاشة جديدة.** لا تسأل «أتعمل؟» (ذاك `doctor`) ولا «ما الذي تحرك؟»
(ذاك `sentinel`) — بل **«أهذه البيانة تناقض أختها؟»**: نهاية دعم مضت
ونظام حي · حرج بلا مالك · حرجية تناقض زمن التعافي · مالك بلا وحدة.

🔑 **ولماذا «يشك في نفسه»**: ⚠️ **فاحص اتساق واثق يخرج مئة ملاحظة صحيحة
نحويا خاطئة واقعا**، فيتعب المالك في أسبوع ويتوقف عن قراءته — **وأداة
تُهمل أسوأ من أداة لا توجد.** فلكل ملاحظة:

1. **حجة** — البيانة التي بنيت عليها، بأسمائها.
2. **حجة مضادة** — السبب المشروع الذي يجعلها ليست خللا.
3. **ثقة محسوبة** من الفجوة الزمنية ونوع القاعدة، **لا مكتوبة بيد**.

وما دون **٦٥٪** يعرض في «شكوك» لا في الصدارة — **ولا يخفى بالكلية**، فمن
يبحث عن سبب عطل يريد الشك كذلك. **ولا يكتب في المستودع حرفا** (يحرسه ع٧٢).

## ④ 📐 المرجع الموحد — `/repo/earef`

**شاشة جديدة.** كانت المراجع متفرقة: نص `قياس` في `qiyas_ref`، والبروفة
في `qiyas_sim`، والجدول في `library`. **فمن أراد «أين نحن من 5.3.2؟»
فتح ثلاث شاشات وجمع بنفسه** — **وجمع يدوي يتكرر يخطئ مرة**.

الآن لكل بند: **نص المرجع · الدليل من المستودع (شاشة ورقم محسوب) ·
والحكم**. ⚠️ **وبند بلا دليل يعلن أنه بلا دليل ولا يحسب منجزا** —
**فنسبة امتثال مبنية على ادعاء أسوأ من نسبة منخفضة صادقة.**

🩸 **واعتراف**: كتبت للبند `5.3.1` دليلا `lambda: 1` — **رقما بيدي يقول
«عليه دليل» وهو لم يقس شيئا**، وهو بعينه ما يحذر منه رأس الملف.
**ومقياس يصدق نفسه لا يقيس.** استبدل بقياس حقيقي (أصدرت الوثيقة فعلا؟).

## ⑤ توجيه التنبيهات إلى مدراء الإدارات

🎯 **وتوجيه كل شيء إلى المالك يجعله صندوق بريد لا مراجعا**: مئة تنبيه
عن مئة إدارة تصل إلى واحد **فلا يقرأ منها شيء**.

`directors.route` + `pulse.fan_out`: نسخة لكل مدير **عن إدارته وحدها**،
**مجمعة** لا رسالة لكل بند (**ورسالة لكل بند تغرق الصندوق فيصير
التنبيه ضجيجا يصفى آليا**). ⚠️ **وبلا مدير لا يرسل** — ترفع للمالك
موسومة بأن الإدارة بلا مدير، **فيصلح السبب لا يعالج العرض**.

## ⑥ المسح البصري

**٤٠ شاشة × مقاسين (سطح وجوال)**: صفر فيض أفقي · صفر خطأ جافاسكربت.

## ⑦ 🩸 والأحمر الذي لم يكن قابلا للإغلاق

حارس ⑤ يميز «نص واجهة» من «محتوى المستودع» بالطول: القصير واجهة
والطويل محتوى. **لكنه كان يقيس العينة المقصوصة عند ٣٤ حرفا** ثم يسأل
«أهي ≤٤٢؟» — **فالشرط يتحقق دائما مهما طال الأصل**.

فكان **نص المعيار الرقابي (٤٠٠ حرف)** و**أسماء الإدارات العربية** تعد
«نص واجهة عربيا متسربا». **وحارس يقيس عينته لا أصله لا يميز شيئا** —
ولذلك بقي هذا الأحمر مفتوحا أياما لا يغلق مهما أصلحنا.

صار يقيس **طول المقطع كاملا**. والنتيجة: ⑤ يجتاز لأول مرة، **وما بقي
عربيا هو ما يجب أن يبقى عربيا** — نص المنظم وأسماء الإدارات.

---

## التطبيق

```bash
cd ~/kamc_ea
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch.tar.gz \
  -pass file:$HOME/.config/kamc/deploy_pass
sha256sum -c <(echo "$(cat latest.sha256)  patch.tar.gz")
./patch.sh patch.tar.gz          # ⛔ بلا sudo
```

**وبعدها قس لا تصدق الرسالة**: `/repo/diag` يقول **2026.08.15-6** ·
`/repo/watchman` و`/repo/earef` تفتحان · **احفظ وثيقة وقس الزمن —
لا يجب أن يتجاوز جزءا من الثانية** · ونزل الوثيقة Word وتأكد أنه لا
مربعات فيها.

## ما بقي عليك

- **حارس قارئ الوثائق** ينتظر منك `refs_public.json` يصنف الوثائق
  عامة/داخلية — **ولا تصنف وثائق المدينة من هنا**.
- **`nginx auth_basic`** على خادم المدينة — وقد سقط عن العجلة، فقد
  تبين أنه بريء من عطب الخروج (أصلح في `-5`).
