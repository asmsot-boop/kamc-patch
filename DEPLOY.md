# نشر 24-4 — patch-2026.08.24-4

> **تجُبّ 24-1 و24-2 و24-3** — طبّقْ هذه وحدها.
> بصمة الرقعة **داخل** التعمية: `ad94ed4da6b2fdd65b87c0ee`.
> سبعةُ ملفّات: `eamm.py` (جديد) · `env.py` · `app.py` · `shell.py` · `ar_allow.py` · `ldap_auth.py` · `BUILD.json`.
> **لا تمسّ البيانات** — والجدولُ الجديد يُنشَأ وحدَه بلا هجرةٍ يدويّة.

## ① 🏛 جديد: شاشةُ نضج ممارسة البنية المؤسسية — `/repo/eamm`

**التقييمُ الذاتيُّ الصادر عن هيئة الحكومة الرقمية** صار داخل المنصّة: **٢٩ سؤالًا في أربعة محاور**، لكلّ سؤالٍ سُلَّمُ مستوياتٍ تصاعديّ يُختار منه واحد، مع حقلِ **سند** لكلّ إجابة.

- **النصُّ حرفيٌّ من النموذج** — الأسئلةُ والخياراتُ كما هي، لا ملخَّصةً ولا معادَ صياغتها.
- **أُدرجت الشاشةُ في قائمة «ما يبقى عربيًّا»** (`ar_allow.py`، صنف «نص المنظم») — **فلا يُترجَم سؤالٌ ولا خيار**، إذ ترجمةُ نصّ المنظِّم تغيّر ما وقّعت عليه الجهة. أمّا زخرفةُ الشاشة (العناوين والأزرار) فثنائيّةٌ كبقيّة المنصّة.
- **بذرةٌ من لقطات المالك 24/8**: ستُّ إجاباتٍ في المحور الأول (س١=٤ · س٢=٣ · س٣=٥ · س٤=٤ · س٥=٣ · س٦=٤) مختومةٌ بسندها. ⚠️ **والبذرةُ لا تدهس تعديلًا**: من غيّر مستوًى في المنصّة لا يُعاد إلى قيمة اللقطة عند أيّ إقلاعٍ لاحق — **مُقاسٌ بالتشغيل** (غُيّر س١ إلى ٢ ثمّ أُعيد التشغيل فبقي ٢).
- **التعديلُ لمن يملك `steward`** فقط، ويُقيَّد في سجلّ التاريخ.
- **ولا نسبةَ مئويّةً تُعرض**: أوزانُ المحاور وطريقةُ الاحتساب لم تصدر لنا، فتُعرض المستوياتُ ومجموعُها والسقف — **ولا رقمٌ نهائيٌّ مخترع**.

⚠️ **ناقصان معلَنان في النصّ نفسه**: المستوى الخامس في السؤالين **١١** و**٢٤** لم يظهر كاملًا في اللقطة، ونصُّ الخيار يقول ذلك صراحةً — يُستكمل من الملفّ الأصليّ.
⚠️ **وهو غيرُ «قياس V5.0»**: `/repo/qiyas` يحمل متطلبات 5.3.1–5.3.4 بترقيمها المستقلّ. **لا تخلطْ رقمَ سؤالٍ هنا برقمِ متطلبٍ هناك.**

## ② 🚪 إصلاحُ تسجيل الخروج (كان في 24-3)

كان زرُّ الخروج يقذف المستخدمَ خارج الموقع: `logout_redirect` يقرّر الوجهةَ من نوع البوّابة وحدَه، ومضيفُ المدينة ليس `*.asmsot.com` فيُحسب `basic` ويحوّل إلى `/repo?bye=1` على افتراض أنّ nginx يصادق — بينما المدينة تصادق **داخل المنصّة** بدخول AD. الآن: **وضعُ AD يسبق منطقَ البوّابة ⇒ `/login?bye=1`** ورسالةُ «سُجّل خروجُك». وسلوكُ Cloudflare وbasic لم يتغيّر حين يكون AD مطفأً.

## ③ 🔑 صفحةُ الدخول المقسومة و④ 🔢 ختمُ النسخة (كانا في 24-1/24-2)

شاشةٌ مقسومة: طبقاتُ المعمار حيّةً + نموذجٌ بحقوله كما هي، بخطّ المدينة وشعارها مضمّنَين (صفر طلباتٍ خارجيّة). والختمُ بـ`build.freeze` فيعود رقمُ الإصدار **أخضر**.

## الفحص قبل الشحن (بالتشغيل لا بالقراءة)

نسخةٌ معزولة (منفذ 5098، قاعدةٌ منسوخة): الشاشةُ تُبنى **٢٩ سؤالًا و١٤٠ خيارًا**، الحفظُ يعمل (POST → 302 والقيمةُ في القاعدة)، البذرةُ لا تدهس، ولا `**` ماركداون في النصّ. و**فحصُ الدخان كامل الشيتات: ٧ سليم · ٠ مختل**.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.24-4.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.24-4.tar.gz | cut -c1-24    # يجب ad94ed4da6b2fdd65b87c0ee
./patch.sh patch-2026.08.24-4.tar.gz
```
وللتراجع: `./patch.sh --rollback`

## بعد التطبيق — أربعةُ تحقّقات

1. **الشاشة**: `/repo/eamm` تفتح وتعرض ٢٩ سؤالًا، وستُّ إجاباتٍ مؤشَّرةٍ في المحور الأول.
2. **الحفظ**: غيّرْ مستوى سؤالٍ واحفظْ — يجب أن يبقى بعد تحديث الصفحة.
3. **الخروج**: زرُّ الخروج يصل بك **صفحةَ الدخول** مع رسالة «سُجّل خروجُك».
4. **رقمُ الإصدار**: **أخضر ✓** ويقرأ «إصدار 4 · 24/8».
