# نشر 19-1 — patch-2026.08.19-1

> **تجُبّ كل ما قبلها** (لقطة جذر كاملة · 171 ملفًّا · النسخة 2026.08.19-1) — تصلح من أي نسخة سابقة.
> بصمة الرقعة **داخل** التعمية: `2666e247d3638527e7a5b25c` — قارنها بعد الفك قبل التطبيق.
> `shared_viz.py.enc` لم يتغير — **لا خطوة يدوية**.

## ما فيها — ثلاث أولويات اعتمدها المالك 19/8

1. **🔗 «قرب الامتلاء» كان متجمّدًا منذ 17/8 — والسبب ليس مهمّة ميتة ولا قارئًا خاطئًا، بل الكاتب لم يُوصَل بمسح الفجر أصلًا.** الفجر كان ينادي `esx_inventory` (REST بلا عدّادات) فيجري كل صباح **ولا يكتب لقطة قط**، والشاشة تبقى على آخر ضغطة زرّ يدويّة. **الجديد `esx_read.py`**: المفاضلة pyVmomi→REST في موضع واحد يخدم الزرّ والمؤقّت معًا (والزرّ حُذف منه المنطق فلا نسخة ثانية). وسطر تقرير الفجر صار يعرض `📈` أو `⚠️ بلا لقطة امتلاء`. **والجمل الثلاث في `usage_watch` التي كانت تَعِد بلقطةٍ مع الفجر صُحّحت** — الوعد الكاذب يُقرأ سلامةً.
2. **📣 سلسلة التنبيه مكوّن ← إدارة ← مدير**: `dept_alerts` توسّعت خارج `kind='gap'` لتشمل **الامتلاء** والتقادم والحرج الخفي · `fan_out` يرسل لكل مدير شريحته · `routing_preview` يعرض من كان سيصله ماذا · **والتوجيه صار بالمعرّف لا باسم الإدارة** (تغيير اسم إدارة كان يقطع التوجيه صامتًا — لا يظهر في شاشة بل في بريدٍ لا يصل).
3. **🧯 سجل الأخطاء صار مقروءًا**: حالة ثالثة (غياب تهيئة · مسبار · حقن اختبار) تُخرج الضجيج من الصدارة **بلا حذف سطر** — المفتوح كعطل **23 بـ221 وقوعًا ← صفر**، و43 بصمة كما كانت. وحزمة الانحدار تنظّف حقنها. **وبقّتان أُصلحتا**: حارس النوع في `i18n.stats` (قيمة نصّية واحدة كانت تُسقط `/repo/i18n` كاملة) · ومعالجات `/repo/twin` تُعرَّف **قبل** إقلاع محرك 3D مع رسالة صريحة إن سقط (كانت شاشة صامتة).

**الاختبارات: 1032 نجح · 0 فشل · 2 متروك** (تحمّل 72 ساعة مقصود) — على قاعدة معزولة.

⚠️ **حدّان يُذكران بصدق**: تشغيل الفجر جرى بوضع محاكاة (pyVmomi غير مثبّتة على الورشة) — **الأنبوب مثبَت طرفًا لطرف والقراءة من vCenter حقيقي تُثبَت هنا على المدينة**. و**Nutanix لم يُبنَ التقاط سعاته** (يحتاج بناءً في قارئ Prism) — وصُرّح به سطرًا في الشاشة فلا يُقرأ الفراغ سلامةً.

## التطبيق (المعتاد)

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.19-1.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.19-1.tar.gz | cut -c1-24    # يجب 2666e247d3638527e7a5b25c
./patch.sh patch-2026.08.19-1.tar.gz
```
وللتراجع: `./patch.sh --rollback`

## بعد التطبيق — ثلاثة تحققات

1. **الامتلاء (الأهم)**: شغّل مسح الفجر (أو انتظر مؤقّت 6:30) ثم افتح `/repo/usage` — **يجب أن يحمل تاريخ اليوم لا 17-08**. وسطر المصادر في تقرير الفجر يجب أن يقول `VMware: N 📈`؛ فإن قال `⚠️ بلا لقطة امتلاء` فالقراءة سقطت إلى REST والسبب مذكور بجانبها.
2. **التنبيه**: `/repo/pulse` — قسم الامتلاء يظهر ضمن بنود الإدارات، ومعاينة التوجيه تعرض من يصله ماذا.
3. **سجل الأخطاء**: `/repo/errors` — تبويبات (عطل · غياب تهيئة · مسبار · حقن اختبار)، وعدّاد «أعطال مفتوحة» يجب أن يكون **صفرًا** بعد التصنيف.
