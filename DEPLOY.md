# رقعة 2026.08.15-4

> 🔐 `-pass file:$HOME/.config/kamc/deploy_pass` — **والكلمة دُوّرت 15/8**،
> فإن لم تضعها على المدينة بعد فسيفشل الفكّ بـ`bad decrypt`. ضعها أوّلًا.

> ⛔ **بلا `sudo`.** والسكربت الآن **يرفض** لا يحذّر — **ورفضُه قاطعٌ بلا
> استقصاء**: كان يستدعي `sudo` ليكشف أنّ الخدمة خدمةُ مستخدم، **وسكربتٌ
> يمنع `sudo` باستدعائه يناقض نفسه** (كشفه حارسُ النشر ⑫).

**أربعةُ أبواب** — وثلاثةٌ منها من ملاحظاتك المباشرة اليوم.

---

## ① الفجوات ← «ما نقص»: طبقتان لا شاشتان

المالك: *«الفجوات فيها لبس بينها وبين شاشة ما نقص وشاشة مجموعات العمل.»*

وقِسنا فبان أنّها ليست تكرارًا بل **حلقةٌ مقطوعة**:

| الشاشة | ما هي فعلًا |
|---|---|
| **ما نقص** | **عدّادٌ آليّ** يمسح المستودع — ١٠١ وحدة بلا أصل · ٣٤١ أصلًا بلا مالك |
| **الفجوات** | **بيانٌ معماريٌّ مكتوب** بأسماءٍ ومسؤولين وتواريخ |
| **مجموعات العمل** | **مرحلةٌ** لا شاشة — تحزم الفجوات مبادرات (الطور E) |

**والعطبُ أنّ العدّاد لا يغذّي البيان**: «ما نقص» يقول ١٠١، وفي الفجوات
فجوةٌ مكتوبةٌ بيدٍ **لا تعرف عددَها**. **رقمان لشيءٍ واحدٍ لا يتحدّثان.**

فصار في «ما نقص» زرّ **🔵 حوّلها فجوات بيانات**، و:

- **يحدّث ولا يكرّر** — تشغيلٌ ثانٍ: `made:0 · updated:3`، والعددُ ثابت.
  **وإلّا صار المستودعُ سجلَّ محاولاتٍ لا سجلَّ حال.**
- **ولا يدهس ما كتبتَ**: المسؤولُ وتاريخُ الالتزام يبقيان بعد كلّ مزامنة.
- **وما اندملَ يُقاعَد** — فجوةٌ صار عدّادُها صفرًا تُغلق، **وفجوةٌ مغلقةٌ
  تبقى معروضةً تُفقِد الشاشةَ صدقَها.**
- ولكلّ فجوةٍ مولَّدةٍ **توأمٌ إنجليزيٌّ يُكتب مع الصفّ** — فالمولِّدُ أولى
  من المترجِم بأن يعرف.

ومجموعاتُ العمل صارت **تبويبًا** داخل الفجوات، وفوقها شريطٌ يدلّ على
الطبقة الأخرى فلا يظنّهما اثنين.

## ② الطباعة: عموديٌّ افتراضًا، والاتجاه بزرّ

المالك: *«الملف يطبع أفقيا وليس عموديا… هل نضيف زرّ عموديًا أو أفقيًا؟»*

**والسببُ حدسٌ في الكود**: `land = has_table(md)` — **وجودُ أيّ جدولٍ يقلب
الصفحة**، ولو جدولَ بياناتٍ من عمودين. صار الحكمُ **بعدد الأعمدة** (≥٥)،
فخمسُ وثائق تعود عموديّة.

**واقتراحُك أصحّ من إصلاحي**، فأُخذ الاثنان: زرّان صريحان **🖨 PDF عمودي**
و**🖨 أفقي** (`?o=p` / `?o=l`). **والحدسُ افتراضٌ فقط — لأنّ خطأه يراه
المستلِم لا نحن.** (قِيس من داخل الـPDF: `596×843` مقابل `843×596`.)

## ③ محرّر الوثائق

المالك: *«عند طلب تحرير ينزل ملفّ `.md` والمفترض يفتح محرّر نصوص»* —
**والمحرّرُ موجودٌ وشغّالٌ منذ ١٣/٨** على `/repo/doc/<k>/edit`، **لكنّ
الشاشة لا تدلّ عليه**. كان الزرّ `📝 نص (تعدله)` يشير إلى تنزيل ملفّ،
**فوعدت التسميةُ بما لا تفعله**. صار زرّان: **✍️ حرّر النص** · **⬇ نزّل النص**.

**وشريطُ الحفظ ملتصقٌ بأسفل النافذة** — كان يقع تحت الطيّ بعد نصٍّ
ارتفاعُه ٦٢vh، **فيحرّر المالك ولا يرى بم يحفظ**. **وزرُّ حفظٍ لا يُرى
كزرٍّ لا وجود له.**

## ④ إسنادُ المدراء من الدليل

المالك: *«بمجرّد أختار إدارة غير معيَّن لها مدير… فقط حقل الرقم الوظيفيّ
وبعدها يسحب الإسناد من AD.»*

الدورةُ صارت: **① إدارةٌ من قائمة الناقصة وحدَها** (١٠٤ الآن، لا ١٠٥
كلُّها — **فقائمةٌ فيها الجميع تجعل الشاشةَ عديمةَ فائدة**) ← **② الرقم
الوظيفيّ وحدَه** ← **③ بطاقةُ تأكيدٍ تعرض ما وجد** (ولا يُحفظ شيءٌ قبل
ضغطةٍ ثانية: **خطأٌ في خانةٍ يُسند مديرًا لإدارةٍ ليست له فيصله بريدٌ ليس
له**) ← **④ مسارُ الإعلام**:

| الحال | ما يُعرض |
|---|---|
| له حسابٌ على المنصّة | ✅ يُعتمد |
| لا حساب | **📧 بالبريد فقط** أو **🪪 وامنحه حساب قارئ** |
| لا بريدَ في الدليل | ⚠️ لن يصله شيء |

**وثلاثةٌ مقصودة:** السحبُ يقرأ **الاسم والبريد والمسمّى والإدارة فقط —
لا هاتفَ ولا مكتبَ ولا مدير** (حكمُ المالك: بياناتٌ شخصيّةٌ لا تُعترض)،
ولا يُكتَب في الدليل شيء · **وتضاربُ الإدارة يُعرَض ولا يُبتلع** ·
**والمنحُ قارئٌ فقط**، والأعلى بابُه `/repo/perms` بقصدٍ ظاهر.

---

## وإصلاحاتٌ من فحص المدينة

- **`/repo/gapwork.csv`** كان بحارس `reader` **وهو إخراجُ بيانات** — نُقل
  إلى `can_export` وأُدرج في `_DUMP_PATHS`. **والفرقُ بين الاطّلاع
  والإخراج جوهريّ**، وكانت المنصّةُ تخالف سياسةً كتبتها بنفسها.
- **`patch.sh`**: النطاقُ مثبَّتٌ (`KAMC_SCTL`) بدل كشفٍ يرتدّ بصمت —
  **فـ`systemctl --user` يحتاج ناقلَ جلسة، وإن غاب `XDG_RUNTIME_DIR` رمى
  «Failed to connect to bus» فابتلعه `2>/dev/null` وارتدّ لنطاق النظام**.
  و**الإيقافُ صار يُقاس بالمنفذ ويُجهض قبل الاستخراج**: لا `set -e` هنا،
  **فـ`stop` بلا حارسٍ يعني أنّ فشلَه لا يوقف شيئًا** — تُستخرَج الملفّاتُ
  فوق خدمةٍ تعمل، **وتردُّ القديمةُ على `wait_ready` فيقول «✅ تمّت»**.

## الحراسة

**ع٦٨** — العدّادُ يغذّي البيان: العددُ رقمٌ لا قائمة · المزامنةُ لا
تضاعف · وما كتبه المالك لا يُدهس · **والفحصُ يمحو ما خلق** (وكان يترك
ثلاثَ فجواتٍ في مستودع المالك فتظهر في تقريره ويحسبها من عمله —
**ومن يفحص ولا ينظّف يزوّر ما يفحصه**).

**الحزمة: ٧٥٠ نجح · ١ فشل.** والأحمرُ الوحيد: تسرّبُ نصٍّ عربيٍّ في العرض
الإنجليزيّ (١٠ يتجاوز الحدّ ٦) — **وأكثرُه قيَمُ بيانات لا نصُّ واجهة**،
وهو في السجلّ بندًا مستقلًّا.

## التطبيق

```bash
cd ~/kamc_ea
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch.tar.gz \
  -pass file:$HOME/.config/kamc/deploy_pass
sha256sum -c <(echo "$(cat latest.sha256)  patch.tar.gz")
./patch.sh patch.tar.gz          # ⛔ بلا sudo
```

**وبعدها قِس، لا تصدّق الرسالة**: `/repo/diag` يقول **2026.08.15-4** ·
`/repo/blindspots` فيه زرّ التحويل · `/repo/gapwork` فيه التبويبات ·
`/repo/docs` فيه **✍️ حرّر النص** وزرّا الاتجاه · `/repo/directors` فيه
قائمةُ الإدارات الناقصة وحقلُ الرقم الوظيفيّ.
