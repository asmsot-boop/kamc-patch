# نشر 25-6 — patch-2026.08.25-6

> **تطبَّق فوق 25-5.** ستّة ملفّات: `usage_alert.py` · `usage_watch.py` ·
> `app.py` · `smoke.py` · `BUILD.json` · `HANDOVER.md`.
> بصمة الرقعة **داخل** التعمية: `9d713b0d8a8aac6a338046df`.

## 📄 ① شاشة الامتلاء بلوكات لا صفحةً واحدة

بلاغ المالك (25/8): *«عدد المعروض جدّا كبير — المفروض ٢٥ عنصرا
والباقي في التالي، فضلا عن الفرز والتصنيفات»*.

⚠️ **وستّمئة صفٍّ في شاشةٍ واحدة لا تُقرأ، فيُلغى النظرُ فيها كلِّها.**

- **٢٥ صفّا في الصفحة**، ومرقّمٌ بنافذةٍ من سبع صفحاتٍ حول الحالية
  **لا مئةُ رقم**، وعدّادٌ يقول «١–٢٥ من ١٢٠».
- **تبويباتٌ للتصنيفات بعددها**: الكلّ · قرص خادم · مخزن بيانات · ذاكرة.
- **خمسةُ فروز**: الامتلاء (الأصل) · السعة · المستهلك · التصنيف · الاسم.
- والحال كلُّه في العنوان (`k` · `s` · `q` · `p`) **فيُشارَك برابطه**.

⚠️ **والبحث صار على كلّ الصفوف لا على المعروض**: كان يُخفي صفوف
الصفحة الحالية وحدها — **فيقول «لا نتيجة» وهي في الصفحة الثالثة**.

⚠️ **ولا فرزَ بالمعالج**: اللقطة **لا تحمل بيانات معالجٍ أصلا** —
قارئ vCenter يجلب الأقراص والذاكرة والمخازن. **وخيارُ فرزٍ بما لا
يُقرأ كذبةٌ في الواجهة**؛ ولو أُريد المعالج فهو عملُ جمعٍ جديد في
القارئ لا خيارَ فرزٍ يُضاف.

⚠️ **وعيبٌ حُصِّن**: `p=abc` في العنوان **كانت تُسقط الشاشة كلَّها** —
فالمَعلَم من المستخدم يُقرأ ويُقصّ ولا يُطاع خامّا.

## 👥 ② وثلاثة أبوابٍ لإضافة المعنيّ

بلاغ المالك: *«المفروض يعرض لي من المستخدمين في النظام أو بالرقم
الوظيفيّ يسحبها من الأكتف دايركتوري»*.

- **بريدٌ حرّ** (كما كان).
- **من مستخدمي المنصّة** — قائمةٌ بالبريد والصفة، **وتُخفي المضاف سلفا**.
- **ومن الدليل النشط** بالاسم أو البريد أو **الرقم الوظيفيّ**، فتظهر
  النتائج بالاسم والرقم والإدارة والمسمّى وزرّ «أضف» لكلٍّ
  (`ldap_auth.search_people` — **قراءة فقط، لا يكتب في الدليل**).

⚠️ **وكتابةُ بريدٍ باليد تُخطئ حرفا فيسكت التنبيه** ولا أحد يدري —
ولذلك البابان الآخران.

⚠️ **والمعطّل لا يُعرض**: فإسنادُ تنبيهٍ لحسابٍ موقوفٍ **يكتم الإنذار
وصاحبُه يحسبه واصلا**؛ وكذلك من بلا بريدٍ فلا قناةَ له.

## المقيس بالتشغيل

على لقطةٍ من **١٢٠ صفّا**: ستُّ صفحات · «٧٦–١٠٠ من ١٢٠» عند الرابعة ·
تبويب المخازن بفرز السعة ٢٠ من ٢٠ · بحثٌ عبر الصفحات ٩ من ٩ · ولا
أخطاء JS. وحُصِّنت المَعالم: `p=abc` · `p=٣` · `p=1e9` · `p=[]` ·
`k=<script>` · `q` بخمسمئة حرف.

## بعد التطبيق

`/repo/usage` — التبويبات وشريط الفرز فوق الجدول، والمرقّم أسفله.
وفي **المعنيّين** ستجد القائمة المنسدلة لمستخدمي المنصّة، **وبحث
الدليل يعمل عندكم** (وهو معطّل في بيئة التطوير فيقول ذلك صراحة).

⚠️ **ولا يزال البريد غير مهيّأ** — فلا تنبيه يخرج ولا بريد تعريفيّ
يصل حتى تُدخل SMTP.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.25-6.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.25-6.tar.gz | cut -c1-24    # يجب 9d713b0d8a8aac6a338046df
./patch.sh patch-2026.08.25-6.tar.gz
```

وللتراجع: `./patch.sh --rollback`
