# نشر دفعة 16/8 الكاملة — patch-2026.08.16-4

> **تجُبّ كل ما قبلها** (لقطة جذر كاملة · 169 ملفا · النسخة 2026.08.16-4 — تشمل 16-3 غير المطبقة).
> بصمة الرقعة داخل التعمية: `7edc4f1d06feeb91cb7e5f19` — **قارنها بعد الفك قبل التطبيق**.
> ⚠️ **هذه الرقعة بجزأين**: رقعة الجذر + **`shared_viz.py.enc`** (إصلاح في المكتبة المشتركة لا يشحنه patch.sh — خطوة يدوية ⑤ أدناه).

## ما فيها (يوم عمل كامل باعتماد المالك بندا بندا)

**من 16-3 (لم تطبق):** 🧲 اقتناص المكرر آليا (`dedupe.py`: مفتاح موحد يقص لواحق النطاق؛ منع في المستوردات الثلاث + دمج تلقائي بعد كل استيراد ومع الفجر؛ الناجي الأغنى باسمه الأقصر؛ تقاعد لا حذف) · 🩺 الفحص الحي من صفحة المكون (`/repo/el/{id}/live`: حالة العنصر وكل عناصر ربطه الآن من vCenter + أسوأ قرص + أقرب مشتبه) · شاشة الامتلاء: جدول موحد ببحث + 🔕 إسكات ذاكرة CVM (محجوزة بالتصميم — كانت أغلب الـ138 إنذارا) + مؤشر الامتلاء برأس كل صفحة وبطاقة باللوحة.

**هندسة القائمة:** 7 مجموعات و~50 بندا بدل 9/84 — دمج تبويبات لا حذف (13 مجموعة تبويب تحقن شريطها آليا: قياس · كاشف الخلل · التغير · توليد العلاقات · القدرات · المبادرات · السجلات المعمارية · المخرجات · المستخدمون · مصادر الربط · التشغيل والصحة · الخرائط · الإدخال) — **كل مسار قديم يعمل** ويضيء بند أمه. أزيلت بنود: المحرر المرئي (حل محله Archi) · المخطط الموقعي (صار تبويبا في الخرائط) · تهيئة المستأجر ومرجع التكوين (يلزمان عند النقل فقط — المساران حيان) · شارة «جديد».

**الصلاحيات — المصفوفة تحكم لا تحفظ:** وسيط `_perm_mw` على كل GET/HEAD: منع صريح في المصفوفة = 403 مقيد بالسجل، والرف والتبويبات وبطاقات اللوحة وشارات الرأس تحجب الممنوع. **وشاشات الإدارة (people/audit/i18n/errors/diag/…) صار عليها حارس قراءة إدارية — كانت مفتوحة لأي مصادق (سد فحص مستقل، حرج)**. الافتراضات لم تتغير: المطلع يرى ما لم يمنع صراحة — التشديد من شاشة المستخدمين شخصا شخصا. + خبيئة 5 ثوان للأداء.

**الملف الشخصي:** لكل مستخدم من اسمه في الرأس (لا بند قائمة): صورة (256px، سقف 2MB، حارس قنبلة فك الضغط) + جوال بتحقق + مفتاح إشعارات **يحترمه توزيع النبض فعلا**. الصورة تظهر بشارة الهوية.

**ضبط واجهة:** الرف المضموم أيقونات لا اختفاء · شريط العرض الأحمر صار رقاقة 🧪 برأس الصفحة (watermark المخرجات باق) · لوحة /repo: وحدات البناء والعلاقات عمودان بخمسة فخمسة وزر «التالي» (وبلوكات الصدر والبطاقات كذلك) · المنظر: نسبة إطار مطابقة + عناوين لا تقص + رص يميني + الاسم يحدد العرض · treemap الداشبورد: العناوين داخل بلاطاتها (إصلاح RTL في shared/viz) · أزرار الرأس صناديق موحدة 38×36 واللوحة/الداشبورد أيقونة بلا لفظ · الروابط النصية مسطرة بلون النظام · ⭐ **نجمة المدينة favicon** (من بذرة الأصول — تصل تلقائيا) · محرر مفردات في شاشة الترجمة (بحث + تحرير مباشر + «راجعته»).

**فحص مستقل (Sonnet) على الدفعة كلها**: 7 عيوب وجدت وأغلقت قبل التحزيم (منها الحرج أعلاه + التفاف HEAD + أداء الرف). المنظومة: smoke 7/7 + الأطقم الستة t_01–t_08 كلها صفر.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.16-4.tar.gz -pass pass:<الكلمة المعتادة>
./patch.sh patch-2026.08.16-4.tar.gz
```

للتراجع: `./patch.sh --rollback`.

## بعد التطبيق — سبع خطوات لكلود المدينة

1. **القائمة الجديدة**: 7 مجموعات، وافتح «قياس» وتنقل بين تبويباته (البروفة/الأدلة/المتطلبات/المرجع الموحد) — الشريط فوق والبند يبقى مضاء.
2. **الصلاحيات**: بهوية موظف viewer جرب `/repo/people` — يجب **403** (كانت مفتوحة). وجرب منعا صريحا من شاشة عبر `/repo/perms` وتأكد أنه ينفذ ويخفي البند.
3. **الاقتناص**: شغل `python3 dedupe.py` مرة يدويا (أو انتظر الفجر) — يدمج أزواج KAMCHISWRS2 وأشباهها؛ تحقق أن الباقي عنصر واحد باسم قصير وفي سماته «أسماء أخرى».
4. **شاشة الامتلاء**: بعد أول قراءة vCenter — عداد الإنذارات هبط (ذواكر CVM موسومة 🔕 لا تنذر) ومؤشر الرأس يعرض أعلى نسبة **بعد استثنائها**.
5. **🧩 المكتبة المشتركة (يدوي — لا يفعله patch.sh):**
```bash
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/shared_viz.py.enc -o shared_viz.py.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/shared_viz.sha256 -o shared_viz.sha256
sha256sum -c <(echo "$(cat shared_viz.sha256)  shared_viz.py.enc")   # يجب OK
cp ~/shared/viz.py ~/shared/viz.py.bak-$(date +%Y%m%d)
openssl enc -d -aes-256-cbc -pbkdf2 -in shared_viz.py.enc -out ~/shared/viz.py -pass pass:<الكلمة المعتادة>
```
   ثم تحقق: بصمة المفكوك تبدأ `77006ed28fad12ea`، وبعد إعادة التشغيل «تركيبة المستودع» في الداشبورد عناوينها داخل بلاطاتها.
6. **تنقية الترجمة**: `/repo/i18n` ← «نظف الشفرة والقمامة» (يزيل قيود البريد المسربة القديمة)، وجرب محرر المفردات الجديد.
7. `systemctl --user restart kamc` مؤكدا (عمر العملية تغير) + `postcheck` (يشمل profile وusage) + الملف الشخصي من الاسم بالرأس + نجمة التبويب (تبويب جديد).

> ⚠️ المتروك عمدا: افتراض الصلاحيات لم يقلب (المطلع يرى ما لم يمنع) — قراره للمالك؛ وأفعال الكتابة بالرتبة لا بالمصفوفة بعد.
