# 🚚 نشرُ رقعة KAMC على خادم المدينة — دليلُ مهمّة

> **أنت كلود كود على جهاز العمل داخل المدينة الطبيّة.** مهمّتُك: فكُّ تعميةِ أحدثِ
> رقعةٍ ونشرُها على خادم المنصّة الحيّة، والتحقّقُ أنّها تعمل. المالكُ لا يريد
> إجراءً يدويًّا — **افعلها كاملةً وأبلِغه بالنتيجة سطرًا واحدًا.**

## 🔑 مهمّ: الرقعةُ مُعمّاة
هذا المستودعُ عامّ، فالرقعةُ فيه **مُعمّاةٌ AES-256** (`latest.tar.gz.enc`). **المالكُ
يعطيك كلمةَ السرّ في رسالته** (لا تُوجد هنا). لا تنشر شيئًا قبل فكِّ التعمية بها.

## الحقائق التي تحتاجها
| | |
|---|---|
| **خادم المدينة** | `kamc@10.11.72.145` (SSH — جهازُك يصله؛ AWS لا يصله) |
| **مجلّد المنصّة على الخادم** | `~/kamc_ea` |
| **الخدمة** | systemd **`--user`** باسم `kamc` (بلا `sudo`): `systemctl --user restart kamc` |
| **أداةُ التطبيق** | `~/kamc_ea/patch.sh` (فيها نسخةٌ احتياطيّةٌ وتراجعٌ تلقائيّ + فاحصُ postcheck) — **اقرأها أوّلًا** |
| **الرقعة المُعمّاة** | `latest.tar.gz.enc` · تفاصيلُها في `latest.sha256` |

## الخطوات
1. **اسحب** (عامّ، بلا تسجيلِ دخول): `git clone https://github.com/asmsot-boop/kamc-patch` (أو `git pull` إن كان مستنسَخًا).
2. **فُكّ التعمية** بالكلمة التي أعطاكها المالك (بدّل `PASSWORD`):
   ```
   openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out latest.tar.gz -pass pass:PASSWORD
   ```
   - لو لم يكن `openssl` متاحًا (ويندوز)، استعمل Git-Bash أو بايثون (`cryptography`/استدعاء openssl). تأكّد أنّ الناتجَ `.tar.gz` صحيحٌ: `tar tzf latest.tar.gz | head`.
3. **انقلها للخادم**: `scp latest.tar.gz kamc@10.11.72.145:~/kamc_ea/`
4. **طبّقها**: `ssh kamc@10.11.72.145`، ثمّ `cd ~/kamc_ea && ./patch.sh latest.tar.gz`.
   - ⚠️ **لا `sudo`** — الخدمة `--user`. لو طلب `patch.sh` صلاحيةً، اقرأه وكيّف (قد يكون التطبيقُ من `data/_staging` ثمّ `systemctl --user restart kamc`).
   - ⚠️ **لا تلمس `data/*.db`** — بياناتُ المدينة الحقيقيّة. الرقعةُ شفرةٌ فقط.
   - ✅ **`patch.sh` يُجري `postcheck.py` تلقائيًّا** (يفحص ٦٥ صفحةً + العلاماتِ الحيّة) ويتراجعُ وحدَه لو انهارت صفحة. راقِبْ مخرجاتِه.
5. **تحقّقٌ نهائيّ** (بعد أن يقول patch.sh «طُبّقت واجتازت الفحص»):
   - `https://ea.kamc.med.sa/doc` → **«٢٣ وحدةَ بناءٍ و١٧ علاقة»** (لا `{KINDS}`).
   - أيّ صفحةِ مستودع → زرُّ **`🔗`** بجوار `؟`.
   - `https://ea.kamc.med.sa/repo/refs` → لا ٥٠٠.
6. **إن فشل**: تراجعْ: `ssh kamc@10.11.72.145 'cd ~/kamc_ea && ./patch.sh --rollback'`، وأبلِغ المالكَ الخطأَ حرفيًّا.
7. **نظافة**: احذف `latest.tar.gz` المفكوكةَ من جهازك بعد النشر (لا تُبقِ نسخةً غيرَ مُعمّاة).

## 🗂 (اختياريّ — عند طلب المالك) قراءةُ جرد AD بعد النشر
لو طلب المالكُ **«الدائرةَ الكاملة»** (نشر + قراءة AD)، بعد نجاح النشر نفّذ على الخادم:
```
ssh kamc@10.11.72.145 'cd ~/kamc_ea && python3 ad_inventory.py --stage'
```
- **قراءةٌ فقط لـAD** (استعلامٌ من الـDC، لا مسحَ شبكة). يحمّل إعدادَ LDAP من drop-in الخدمة تلقائيًّا.
- يطبع ملخّصًا: «خوادمُ حيّة N · إدارات M · ميّت K». **لا يحقن شيئًا في المستودع** — يحضّره فقط.

- ⚠️ **حلُّ الـIP مُطفأٌ افتراضيًّا** (آمن): القراءةُ من الـDC فقط، بلا استعلامات DNS مكثّفة. لتفعيله لاحقًا (خارج الدوام بإذن): `KAMC_AD_RESOLVE_IPS=1 python3 ad_inventory.py --stage`.
- ثمّ المالكُ يفتح `https://ea.kamc.med.sa/repo/ad` → يراجع → يستورد المختار.
- لو فشل بـ«LDAP غيرُ مُهيَّأ»: تأكّد أنّ خدمةَ kamc تعمل وأنّ drop-in الـLDAP موجودٌ (`~/.config/systemd/user/kamc.service.d/`).

## ✅ تحقّقُ الرقعة تحديدًا (بعد postcheck)
- شارةُ الإصدار أسفلَ الرفّ **خضراءُ ✓** الآن `v2026.08.08-5 · ... ✓` (لا برتقاليّة — أُصلح بقائمةِ ملفات؛ ملفٌّ زائدٌ لا يقلبها).
- `/repo/materialize` يعرض **«اقتراحاتٌ جاهزة»** (سماتُ الإدارة الأمّ → owns). اضغط «شغّل التحويل».
- العرضُ يملأ الشاشةَ نسبيًّا · حوارُ السكون (تمديد/خروج) بعد 13.5د · زرُّ 💥 أثر التوقّف منسّق.

## بعد النجاح
أبلِغ المالكَ سطرًا واحدًا: **«✅ نُشرت الرقعةُ · postcheck مرّ · [وإن طُلبت قراءةُ AD:] AD جاهزٌ في /repo/ad للمراجعة.»**
