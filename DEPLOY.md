# نشر 24-13 — patch-2026.08.24-13

> **تطبَّق فوق 24-12.** ثلاثة ملفات: `eamm.py` · `app.py` · `BUILD.json`.
> بصمة الرقعة **داخل** التعمية: `0bd68e33045d0debad1547ed`.

## 📊 جدول الفجوة إلى الجواب المثالي

**طلب المالك:** «وش الجواب اللي أنا عليه اليوم، وش الجواب المثالي، وإيش اللي لازم أسويه عشان أوصل له — كجدول: المحقق للمثالية يعطيني الاستشهاد، وغير المحقق يقول لي الإجراء التالي».

**الشاشة:** `/repo/eamm?view=gap` (وزرّ تبديل أعلى شاشة التقييم).

| العمود | ما فيه |
|---|---|
| # | رقم السؤال في النموذج |
| السؤال | نصّه حرفا |
| **اليوم** | مستواك من السقف — أخضر ✅ لمن بلغ، وكهرمانيّ لمن دونه، و«—» لمن لم يُقيَّم |
| **الجواب المثالي** | نصّ المستوى الأعلى الذي يعطي الدرجة الكاملة |
| **الإجراء المطلوب** | خطوات مرقّمة للوصول إليه — أو **الاستشهاد** لمن بلغه |

⚠️ **والإجراء ليس رأينا**: هو **نصّ المستويات الأعلى حرفا** كما ورد في النموذج. **فمن صاغ الإجراء بكلامه أوصل الجهة إلى جواب لا يطابق ما وقّعت عليه** — والمقيّم يقرأ نصّه لا نصّنا.

**ولمن بلغ السقف** يُعرض: «بلغتَ الدرجة الكاملة» + **الاستشهاد المطلوب عند التقديم** بعدّاد حيّ من المستودع وشاشته. ولمن دونه: رابط «ما عندك اليوم في».

**وبطاقات أعلى الجدول:** كم سؤالا بلغ المثالي · كم دونه ولها إجراء · كم بلا تقييم · مجموع المستويات من السقف.

**وطباعة نظيفة** — الجدول يُطبع بلا أزرار.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.24-13.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.24-13.tar.gz | cut -c1-24    # يجب 0bd68e33045d0debad1547ed
./patch.sh patch-2026.08.24-13.tar.gz
```

ℹ️ **وكلما رفعتَ مستوى سؤال في شاشة التقييم، تغيّر جدول الفجوة تلقائيا** — فهو يُحسب لحظة العرض لا يُخزَّن.
