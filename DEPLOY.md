# نشر 24-6 — patch-2026.08.24-6

> **تطبَّق فوق 24-5** (لا تجبّها — تكملها). ستة ملفات.
> بصمة الرقعة **داخل** التعمية: `c9920905e140715e2d219dff`.

## 🔐 الخزنة تصل القراء أخيرا — وكانت واجهة بلا سلك

**عتب المالك:** «ليش مو في الخزنة البيانات؟ الاتفاق المسبق أن تودع في الخزنة وتصنف من هناك وتسحب».

**والعطب كان أكبر مما بدا:** `vault.env_for` — الدالة التي تسلّم بيانة الاعتماد للقارئ — **كانت معرّفة ولا يستدعيها أحد إطلاقا** (صفر مستدعٍ في المستودع كله). فالخزنة تخزّن وتعمّي وتعرض حالها، **والقراء الأربعة يقرؤون من متغيرات البيئة** — فكلمةٌ تُدخل من الشاشة **لا تصل قارئا أبدا**. الاتفاق كان مبنيا نصفه.

**ما تغير:**
- **`vault.creds(scope)`** هي الوصل، **ينادها كل قارئ أولا**، ويبقى متغير البيئة **احتياطا** فلا يتعطّل من هيّأ قبل الخزنة.
- وُصل: **ويندوز · vCenter · Prism**.
- **نطاق `win` أُضيف للخزنة**: حقل المضيف يقبل **عناوين مفصولة بفواصل**، واختبار الاتصال يجرّب المنفذ 5985.
- شاشة الجرد صارت **تدل على الخزنة** لا على ملف البيئة.

⚠️ **وعطب أسبقية التقطه التشغيل لا القراءة**: بلا أقواس يلتصق `.split` بمتغير البيئة وحده **فتُفكَّك قيمة الخزنة حرفا حرفا** (`['1','0','.','1'…]`) — أُصلح.

## الفحص (بالتشغيل)

إيداع بيانة في الخزنة ← **وصلت القارئ** (عنوانان وحساب) ← الكلمة **معمّاة في القاعدة** (`gAAAAA…`) ولا تظهر في الشاشة ← دورة سحب كاملة تعيد «قُرئ ١ · ينتظر اعتمادا ١». وشاشات `/repo/vault` و`/repo/win` و`/repo/sources` كلها 200. **والدخان ٧ سليم · ٠ مختل.**

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.24-6.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.24-6.tar.gz | cut -c1-24    # يجب c9920905e140715e2d219dff
./patch.sh patch-2026.08.24-6.tar.gz
```

## بعد التطبيق — التهيئة من الشاشة لا من الطرفية

1. افتح **`/repo/vault`** ← اختر نطاق **«خوادم ويندوز (WinRM)»**.
2. أدخل: **المضيف** = `10.11.72.71` (أو عدة عناوين بفواصل) · **المنفذ** = `5985` · **الحساب** · **كلمة السر**.
3. اضغط **اختبار** — يجب أن يقول «المنفذ 5985 مفتوح · وحساب مخزن».
4. ثبّت مكتبة القراءة: `cd ~/kamc_ea && .venv/bin/pip install pywinrm`
5. افتح **`/repo/win`** — تصير الحالة «مهيأ» ← **«اسحب الآن وطبّق»**.

✅ **وبهذا لا تمر كلمة السر بطرفية ولا بسطر أمر ولا بأي محادثة** — تعبر متصفح المالك إلى قاعدة المدينة معمّاة، ويقرؤها القارئ في الذاكرة وحدها.
