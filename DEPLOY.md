# نشر 17-3 — patch-2026.08.17-3

> **تجُبّ كل ما قبلها** (لقطة جذر كاملة · 170 ملفا · النسخة 2026.08.17-3) — المدينة على 17-2.
> بصمة الرقعة داخل التعمية: `bca5946a32c476001b9e3276` — قارنها بعد الفك قبل التطبيق.
> `shared_viz.py.enc` لم يتغير — **لا خطوة يدوية**.

## ما فيها — إصلاح عاجل بطلب المالك (يعمل على الربط الآن)

1. **🔗 نوع العلاقة يفلتر قائمة الطرف الآخر**: في شاشة إضافة العلاقات، اختيار «تملك» من تطبيق يعرض الإدارات وحدها لا كل المكونات (كانت القائمة مقصوصة عند 400 اسم فلا تصل الإدارات أصلا). القائمة تعرض علاقات نوع العنصر وحدها، وتتبدل مع كل اختيار.
2. **⚖️ عقد العلاقة يفرض أطرافه خادميا**: الربط من الطرف الوجهة كان **يخزن العلاقة معكوسة** («تطبيق يملك إدارة»)، وأي نوعين كانا يقبلان. الآن: الاتجاه المعكوس يقلب تلقائيا للصواب، والأنواع الخارجة عن العقد ترفض برسالة تسمي العقد المطلوب.
3. **🩺 postcheck صار 80 صفحة**: يغطي `/doc` (التي عاش فيها انهيار SH خارج تغطيته) ووثيقة التسليم ومحرر الوثيقة.

## التطبيق (المعتاد)

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.17-3.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
./patch.sh patch-2026.08.17-3.tar.gz
```

## بعد التطبيق — تحققان

1. افتح عنصرا نوعه تطبيق ← علاقات ← اختر «يملك»: قائمة الطرف الآخر تعرض الإدارات، واختر «تدعم»: تتبدل للعمليات.
2. اربط من تطبيق علاقة «يملك» إلى إدارة ثم افتح العلاقة: المخزن «الإدارة تملك التطبيق» (الاتجاه الصحيح).

بعد النجاح أبلغ المالك سطرا: **«✅ نُشرت 17-3 · postcheck مرّ (80)»**.
للتراجع: `./patch.sh --rollback`.
