# رقعة 2026.08.15-8 — **القائمة الجانبية والفاصلة**

> 🔐 `-pass file:$HOME/.config/kamc/deploy_pass` · ⛔ **بلا `sudo`**.

**تكملة `-7`، وكلها من فحص المدينة المستقل — أصاب في ثلاثة وأخطأت في واحد.**

---

## ① القائمة الجانبية: **٢٨ مسارا بلا ترجمة**

فحص المدينة أثبت أن `«من يصله البريد ومن يصعد إليه المتأخر»` — وصف
`/repo/directors` — **يظهر في كل شاشة بلا استثناء**. وطريقة إثباته
أذكى من فحصنا: **أخذ تقاطع الكلمات عبر ثمان شاشات متباعدة**، فما
تشترك فيه كلها هو الهيكل لا المحتوى.

⚠️ **والقائمة تظهر في كل صفحة**، فسطر واحد غير مترجم فيها **يلاحق
المستخدم في كل ما يفتح**. **وعطب في المشترك يضرب الكل** — ولذلك بدا
كأنه في عشر شاشات وهو في ملف واحد.

فحصنا الجرد كاملا: **٢٨ مسارا في `SPACES` بلا مدخل في `PAGE_EN`**
(`vista` · `watchman` · `earef` · `perms` · `vault` · `logs` · `ops`…).
تُرجمت كلها اسما ووصفا.

## ② الفاصلة العربية `،` بين كلمات إنجليزية

`Process، Application Function، Stakeholder، Driver…`

🔑 **وهذا اكتشاف يستحق أن يكتب**: الكلمات **كلها إنجليزية**، والعربي هو
**المحرف بينها وحده**. ⚠️ **فيفوت أي كاشف يعد الكلمات العربية** —
ولا يظهر في أي مقياس بنيناه. **ومن يعد الكلمات لا يرى الفواصل.**

بُنيت `terms.sep()` و`terms.join()` تتبعان لغة العرض، **بدل `«، »`
مكتوبة حرفا في الشفرة**.

## ③ ونقده لمنهجنا — وهو صادق

قال إن المقياس السابق **كان يقص العينة عند ٣٤ حرفا ثم يسأل «أهي ≤٤٢؟»
— فيتحقق دائما**. وقد صححناه في `-7` قبل رسالته، **لكنه وصل إليه
مستقلا** — وهذا يزيد الثقة فيه لا ينقصها.

## 🩸 وخطأ منا يستحق التسجيل

قلنا «صفر أحمر» وفي `/repo` بالإنجليزية **٢٦ فاصلة عربية**. فحصناها:
**كلها في تعليقات CSS داخل `<style>`** — لا يراها المستخدم، والكاشف
يستبعدها بحق. **فالبوابة كانت صادقة، والشك كان في محله لكن النتيجة
برأتها.** ⚠️ **ولولا أننا فحصنا لظن أحدنا أن البوابة تكذب فعطلها** —
**وحارس يعطل بالشك أسوأ من حارس يخطئ.**

## وما بقي عربيا عمدا

أربعة في `/repo/watchman`: **أسماء إدارات من المستودع** داخل جملة
إنجليزية (`«إدارة الأشعة» is written as the owner…`). **بيانة مسموحة —
ودين يسقط يوم يملأ `name_en` لها**، لا إعفاء دائم.

**الحزمة: ٨٦٠ نجح · صفر فشل.**

---

## التطبيق

```bash
cd ~/kamc_ea
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch.tar.gz \
  -pass file:$HOME/.config/kamc/deploy_pass
sha256sum -c <(echo "$(cat latest.sha256)  patch.tar.gz")
./patch.sh patch.tar.gz          # ⛔ بلا sudo
```

**وبعدها**: `/repo/diag` يقول **2026.08.15-8** · **بدل اللغة للإنجليزية
وانظر القائمة الجانبية** — يجب أن تكون كلها إنجليزية، اسما ووصفا.
