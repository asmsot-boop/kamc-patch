# نشر 24-16 — patch-2026.08.24-16

> **تطبَّق فوق 24-14.** سبعة ملفّات: `eamm_req.py` (جديد) · `eamm.py` ·
> `app.py` · `smoke.py` · `errors.py` · `BUILD.json` · `HANDOVER.md`.
> بصمة الرقعة **داخل** التعمية: `ad4aab37c01eaacbafc559da`.

## 🧭 محرّك متطلبات النضج — شاشة ثالثة على `/repo/eamm`

طلب المالك: «المنصّة فيها بيانات تحلّ بعض الأسئلة — المفروض محرّك ذكيّ
بسيط يعكس بيانات المنصّة ويوائمها مع المتطلبات، **بدون ما يسرّب أو
يخرج من المنصّة**، ولو في قصور يدلّني على معالجته من المنصّة».

**ما يفعله:** كلّ مستوى في أسئلة الهيئة فُكِّك **متطلبات**، ولكلّ متطلب
**مجسّ** يستعلم جدولا في قاعدة المنصّة ويعيد نعم أو لا **ومعه ما وجده
نصّا**. فما تثبته المنصّة يظهر ✅ بدليله، وما ينقص يظهر ⛔ ومعه **رابط
الشاشة التي يُعالَج منها** و**سؤال المقيّم التالي** على ذلك المتطلب.

⚠️ **ولا نموذج لغة في هذا المسار إطلاقا** — اللغويّ يخمّن ويجمّل،
**والإقرار الرقابيّ لا يحتمل تخمينا**؛ والحكم يُعاد إنتاجه بالضبط في
كلّ تشغيل ويصمد أمام سؤال «من أين هذا الرقم؟».

⚠️ **ولا تخرج بيانة واحدة**: المجسّات تعمل داخل عملية المنصّة على
قاعدتها، **ولا شبكة ولا طرف ثالث**؛ وما يبلغ الشاشة عدّاد وحالة لا
محتوى.

⚠️ **والمحرّك يقترح ولا يقرّ**: يعرض «مؤهَّل للمستوى س» ودليلَه،
**والضغط على «اعتمده في التقييم» هو ما يكتب المستوى** — فلا يُملأ
إقرار أمام جهة رقابية إلا بيد صاحبه (وهي قاعدة 24-14 نفسها).

**المستوى المؤهَّل تراكميّ**: أعلى مستوى تحقّقت كلُّ متطلباته وما دونه؛
**ولا يُقفز فوق ناقص**، فمن ادّعى الأعلى سُئل عن الأدنى.

## 🔒 وفحص أخطاء مؤجَّل يُشحن معها (errors.py)

بند **DB-05** من تدقيق كودكس 22/8: فهرس فريد على بصمة الخطأ، **وقبله
دمج أيّ توائم قائمة**. ⚠️ **وعلى المدينة قد تكون وقعت فعلا.**

**مقيس بالتشغيل** على قاعدة توائم مصنوعة: ثلاثة صفوف ببصمة واحدة
(٣+٤+١ ضربة) دُمجت في صفّ واحد بثماني ضربات، **ولم تضع ضربةٌ واحدة**
(المجموع ١٠ قبل وبعد)، والتشغيل الثاني بلا أثر.

## بعد التطبيق

افتح `/repo/eamm?view=req`. المتوقّع على بيانات المدينة: ثمانية عشر
سؤالا معروضا، وأكثر المتطلبات المتحقّقة من **الوثائق الاثنتي عشرة التي
تولّدها المنصّة** (الميثاق · المبادئ · النموذج العام · المناظير ·
الوضع الحالي · الفجوات · خارطة الطريق · السياسة).

⚠️ **والأرقام تختلف عن بيئة التطوير** — فالمجسّ يقرأ قاعدة المدينة لا
قاعدتنا. وهذا هو المقصود.

⚠️ **وفي وضع الإنجليزية** تظهر نصوص الشاشة عربيّةً غير مترجمة، شأن كلّ
شاشة جديدة حتى تمرّ على حصاد الترجمة في `/repo/i18n`.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.24-16.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.24-16.tar.gz | cut -c1-24    # يجب ad4aab37c01eaacbafc559da
./patch.sh patch-2026.08.24-16.tar.gz
```

وللتراجع: `./patch.sh --rollback`
