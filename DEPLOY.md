# نشر رقعة 16/8 الكبرى — patch-2026.08.16-1

> **تجُبّ هذه الرقعة كل ما قبلها** (لقطة جذر كاملة · 165 ملفا · النسخة 2026.08.16-1).
> بصمة الحزمة داخل التعمية: `807a65649d3af3f20404d767` — **قارنها بعد الفك قبل التطبيق**.

## ما فيها (مختصرا)

- **بوابة ArchiMate المعتمدة** (`/repo/archimate`): تصدير يجتاز XSD الرسمي + استيراد من Archi (معاينة→تطبيق، بلا حذف أبدا).
- **17 منظرا قياسيا** (`/repo/viewpoints`) · **أطوار TOGAF بشواهد** (`/repo/togaf`) · **أدلة قياس ونورا** (`/repo/qiyas/evidence` + PDF للهيئة) · **تهيئة المستأجر** (`/repo/tenant`) · **مرجع التكوين** (`/repo/config`) · **الوثيقة المرجعية الحية** (`/repo/handover`) · **واجهة `/api/v1`** الموثقة.
- نوعا الطبقة المادية (منشأة/معدة) + 3 علاقات معيارية (يستدعي/يخصص/يضم).
- إصلاحات ثلاث جولات فحص مستقلة + اختبار نشر بثلاثة نماذج: المنظومة **1145 تأكيدا · 0 فشل**، بروفة رقعة على شفرة وقاعدة 2/8 الحقيقيتين، ترحيل **صفر فقد**.
- كل الشاشات الجديدة موسومة **New** برتقالية في القائمة.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.16-1.tar.gz -pass pass:<الكلمة من المالك>
./patch.sh patch-2026.08.16-1.tar.gz
```

- المطبق يأخذ نسخة للشفرة والقاعدة تلقائيا قبل اللمس، ويتراجع وحده إن لم تستجب المنصة. للتراجع اليدوي: `./patch.sh --rollback`.

## بعد التطبيق — يتحقق كلود المدينة من أربعة

1. **أول إقلاع يبذر الجديد تلقائيا**: مجلد `xsd/` (مخططات The Open Group) و`HANDOVER.md` يعاد بناؤهما من `asset_seed.json` — تأكد: `ls xsd/` بعد الإقلاع.
2. `postcheck` صار يشمل الشاشات التسع الجديدة — شغله واقرأ خلاصته.
3. `lxml` غير مثبتة على المدينة غالبا: شاشة البوابة **تصارح** بأن التحقق معطل (التصدير يعمل) — إن توفر إنترنت مؤقت: `pip install --break-system-packages lxml==6.1.1` ثم أعد التشغيل فتظهر شارة الاجتياز.
4. القاعدة تترحل وحدها عند الإقلاع (جدول tenant + أعمدة جديدة) — **لا لمس يدويا للقاعدة**، وبروفة الترحيل على نسخة 2/8 الحقيقية مرت بصفر فقد.

> ملفا `shared/` (viz/trail/fluid) **لم يتغيرا في هذه الرقعة** — لا خطوة يدوية.
