# نشر 17-2 — patch-2026.08.17-2

> **تجُبّ كل ما قبلها** (لقطة جذر كاملة · 170 ملفا · النسخة 2026.08.17-2) — المدينة على 17-1.
> بصمة الرقعة داخل التعمية: `fb540eadafc1d10e20a122ae` — قارنها بعد الفك قبل التطبيق.
> `shared_viz.py.enc` لم يتغير منذ 17-1 (طُبّق صباح اليوم) — **لا خطوة يدوية**.

## ما فيها — دفعتا 17/8 (كلتاهما بفحص نموذج مستقل قبل التحزيم)

### ① إصلاح الوضع الإنجليزي المنهجي (تشخيص المالك: «خطأ منهجي في استعراض المفردات»)
1. **~950 مدخلة قاموس أعيدت كتابتها تحريريا**: لا جمل تبدأ بـAnd المنسوخة من الواو، ولا ترجمات حرفية — إنجليزية مؤسسية سليمة. و**17 مدخلة كان إنجليزيها بلا صلة بأصلها** (فساد مواءمة قديم) صُححت.
2. **بذرة الترجمة اكتملت**: 65 تسمية رف كانت بلا ترجمة بُذرت، والبذرة تُبنى الآن بدمج صحيح (لا تُسقط مفاتيح).
3. **جرد التسريب صار صادقا**: كان يبتلع 403 ويعدّ الصفحات المقصودة لا المجلوبة — فأي «صفر تسريب» سابق كان بلا سند. الآن يقرأ بريد المالك من drop-in الخدمة، يصرّح بالفاشل، ويتجاهل الملفات الثنائية. القياس بعد الإصلاح: **111 صفحة EN كلها مجلوبة · صفر تسريب غير مبرر** (المبرر مسجل بسببه في `ar_allow` — ومنه `/repo/handover`: نص الوثيقة المعتمدة لا يُترجم).
4. **الاتجاهات**: مصفوفة العلاقات وصفحات العرض البصري تتبع لغة العرض، مع حارس `html[dir=rtl]` يُبقي العربية RTL كما هي.
5. في الطريق: صفحة دراسة الجدوى كانت تنهار 500 (استيراد ناقص) — أُصلحت؛ و`/api/v1` لم تعد تُحسب «مسارا ميتا» في جرد السوق؛ وتجريد تشكيل من 4 ملفات.

### ② دورة الوورد الموثقة (طلب المالك)
في محرر أي وثيقة (`/repo/doc/<key>/edit`): قسم **«📤 دورة الوورد الموثقة»** —
- نزّل Word (الزر القائم) → عدّل في وورد → ارفع الملف → **مسودة لا تمس النص الحالي** → عاين → «✅ اعتمد» (معاملة ذرية) أو «🗑 أهمل».
- كل خطوة تقيد في سجل الأثر **ببصمة الملف**، وجدول `doc_text_versions` يؤرخ كل نص (محرر/Word/مولد · الحالي/منسوخ) — السابق لا يُمحى.
- يرفض غير docx والملف التالف وما فوق 8MB. الجدول الجديد ينشأ وحده عند أول استعمال.

## التطبيق (المعتاد)

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.17-2.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
./patch.sh patch-2026.08.17-2.tar.gz
```

## بعد التطبيق — أربعة تحققات

1. **الوضع الإنجليزي** (زر 🌐): اللوحة والدليل وصفحة الجودة — جمل إنجليزية سليمة لا تبدأ بـAnd، ولا شذرات عربية وسط الإنجليزي (عدا سجلات الأخطاء/النسخ والوثيقة المرجعية — عربية بتصميم مقصود).
2. **الوضع العربي**: مصفوفة العلاقات (Matrix) ما زالت من اليمين لليسار كما كانت.
3. **دورة الوورد**: افتح `/repo/doc/charter/edit` — قسم «📤 دورة الوورد الموثقة» ظاهر بنموذج رفع وسجل نسخ.
4. `/repo/study` (دراسة الجدوى) تفتح 200 لا 500.

بعد النجاح أبلغ المالك سطرا واحدا: **«✅ نُشرت 17-2 · postcheck مرّ»**.

للتراجع: `./patch.sh --rollback`.
