# نشر رقعة الفحص الحي والاقتناص — patch-2026.08.16-3

> **تجُبّ هذه الرقعة كل ما قبلها** (لقطة جذر كاملة · 168 ملفا · النسخة 2026.08.16-3).
> بصمة الحزمة داخل التعمية: `cd3dff835487073037989335` — **قارنها بعد الفك قبل التطبيق**.
> المدينة الآن على `2026.08.16-2` — هذه تضيف فوقها (اعتماد المالك للشاشات 16/8).

## ما فيها (فوق 16-2 المطبقة)

- **📈 شاشة قرب الامتلاء أعيدت** على طلب المالك: **جدول واحد موحد** (النوع عمودا، الأخطر أولا) + **سطر بحث فوري** يصفي الصفوف + **مؤشر امتلاء في رأس كل صفحة** (أعلى نسبة، بلون العتبة) + **بطاقة في اللوحة الرئيسية**.
- **🔕 إسكات ذاكرة CVM** (ملاحظة مراجعة المدينة): محجوزة كاملة بالتصميم — تعرض موسومة في الجدول **ولا تنذر ولا ترفع المؤشر**، فالمؤشر يصدق.
- **🛡 حارس رتبة** على `/repo/usage` (مطلع فأعلى) — كان بلا حارس (ملاحظة المراجعة).
- **🧲 اقتناص المكرر آليا** (أمر المالك): مفتاح موحد يقص لواحق النطاق (`KAMCHISWRS2.kamc.med.sa` ≡ `KAMCHISWRS2-kamc` ≡ `KAMCHISWRS2`) — يمنع التكرار في الاستيراد، **ويدمج القائم تلقائيا** بعد كل استيراد ومع مسح الفجر: الناجي الأغنى باسمه الأقصر، سمات المكرر تكمل فراغاته، والمكرر **يتقاعد لا يحذف** وكل دمجة في سجل الحركات.
- **🩺 الفحص الحي** (أمر المالك): زر «افحص حيا الآن» في صفحة كل عنصر — يسأل vCenter لحظتها عن العنصر وكل عناصر ربطه (مشغل/مطفأ · المضيف وصيانته · Tools · أسوأ قرص من آخر لقطة) ويصدر «أقرب مشتبه به» — لاستكشاف مصدر العطل من شاشة واحدة. المسار `/repo/el/{id}/live`، والنداء في منفذ لا على خيط الخدمة.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.16-3.tar.gz -pass pass:<الكلمة المعتادة>
./patch.sh patch-2026.08.16-3.tar.gz
```

للتراجع: `./patch.sh --rollback`.

## بعد التطبيق — يتحقق كلود المدينة من خمسة

1. `/repo/usage`: الجدول الموحد + البحث يعمل + **عداد الإنذارات هبط** (ذواكر CVM ما عادت تحسب — كانت أغلب الـ138).
2. **رأس أي صفحة** يظهر مؤشر الامتلاء (أعلى نسبة **بعد استثناء CVM** — المتوقع ≈ نسبة أعلى مخزن) واللوحة الرئيسية فيها البطاقة.
3. **الاقتناص**: شغّل `python3 dedupe.py` يدويا مرة (أو انتظر مسح الفجر) — سيدمج أزواج KAMCHISWRS2 وأشباهها ويطبع العينة؛ تحقق أن الباقي عنصر واحد باسم قصير وفي سماته «أسماء أخرى».
4. **الفحص الحي**: افتح صفحة خادم معروف ← «🩺 افحص حيا الآن» — حالته وحالة عناصر ربطه لحظية، والفحص قراءة محضة.
5. جدول usage_snap وبقية الجداول تترحل وحدها عند الإقلاع — لا لمس يدويا للقاعدة.

> ملفات `shared/` لم تتغير — لا خطوة يدوية.
