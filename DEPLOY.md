# نشر رقعة رقيب الامتلاء — patch-2026.08.16-2

> **تجُبّ هذه الرقعة كل ما قبلها** (لقطة جذر كاملة · 166 ملفا · النسخة 2026.08.16-2).
> بصمة الحزمة داخل التعمية: `8785114f05dcefc2c5281205` — **قارنها بعد الفك قبل التطبيق**.
> سابقتها `patch-2026.08.16-1` (بصمة `807a65649d3af3f20404d767`) **مطبقة على المدينة 16/8** — هذه تضيف فوقها.

## ما فيها (فوق رقعة 16/8 الكبرى المطبقة)

- **📈 رقيب الامتلاء** (`/repo/usage` — بطلب المالك 16/8): قارئ vCenter صار يجلب **المستهلك لا المخصص** — الذاكرة النشطة (quickStats) وأقراص الخوادم من الداخل (guest.disk، تشترط VMware Tools) وسعة/حر مخازن البيانات. لقطة مع كل قراءة (يدوية أو مسح الفجر) في جدول `usage_snap` (آخر 90 لقطة).
- **عتبتان**: تحذير 80٪ · حرج 90٪ — تضبطان بالبيئة `KAMC_USAGE_WARN`/`KAMC_USAGE_CRIT` (موثقتان في `/repo/config`).
- **قسم جديد في النبض**: «قرب الامتلاء» ينذر بالأسماء فوق العتبتين (والمخزن الممتلئ أخطر — يجمد كل خوادمه).
- **الصراحة قبل الرقم**: خادم مشغل بلا قراءة أقراص يظهر باسمه وسببه (Tools متوقفة / تعمل ولا تبلغ) — لا فراغ يظن سلامة.
- **هوية المالك**: الاحتياطي المضمن صار `alotaibi@kamc.med.sa` (بريد العمل) — وضبط `KAMC_OWNER_EMAIL` في drop-in يبقى مقدما عليه.
- **نظافة الترجمة**: سلاسل فيها بريد (هوية محصودة خطأ) ترفض حصادا وتزال بزر التنقية — **بعد التطبيق شغل «تنظيف الكود» في `/repo/i18n`** لإزالة القيود المسربة القديمة.
- فحص مستقل (Sonnet) وجد 4 عيوب أصلحت قبل التحزيم؛ المنظومة الاختبارية كلها خضراء (smoke 7/7 + الأطقم الستة exit 0).

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.16-2.tar.gz -pass pass:<الكلمة المعتادة>
./patch.sh patch-2026.08.16-2.tar.gz
```

- المطبق يأخذ نسخة للشفرة والقاعدة تلقائيا قبل اللمس، ويتراجع وحده إن لم تستجب المنصة. للتراجع اليدوي: `./patch.sh --rollback`.

## بعد التطبيق — يتحقق كلود المدينة من أربعة

1. **`/repo/usage` تفتح 200** وتقول «لا لقطة بعد» — اللقطة الأولى تكتب مع أول قراءة vCenter: افتح جرد VMware ← «اقرأ الآن» (pyVmomi)، ثم عد للشاشة فترى الأقراص والذاكرة والمخازن بنسب امتلائها.
2. **النبض** (`/repo/pulse`) يظهر قسم «📈 قرب الامتلاء» بعد أول لقطة.
3. جدول `usage_snap` ينشأ تلقائيا عند الإقلاع (bootstrap) — **لا لمس يدويا للقاعدة**.
4. **تنقية الترجمة**: `/repo/i18n` ← «تنظيف الكود» — يزيل قيود البريد المسربة القديمة (الفلتر الجديد يمنع عودتها).

> ملفات `shared/` **لم تتغير في هذه الرقعة** — لا خطوة يدوية.
