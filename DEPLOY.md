# نشر 24-3 — patch-2026.08.24-3

> **تجُبّ 24-1 و24-2** — طبّقْ هذه وحدها.
> بصمة الرقعة **داخل** التعمية: `5bf88396af4d8684fd36fa1a` — قارنها بعد الفكّ قبل التطبيق.
> أربعةُ ملفّات: `env.py` · `app.py` · `ldap_auth.py` · `BUILD.json`. **لا تمسّ البيانات** (`data/` · `refs/` · `outputs/` · `evidence/`).

## ① 🚪 إصلاحُ تسجيل الخروج — العطلُ الذي أبلغ عنه المالك بمقطعٍ من المدينة

**ما كان يحدث:** يضغط المستخدمُ زرَّ الخروج فيخرج **من الموقع كلِّه** إلى صفحةٍ خارجيّةٍ بلا خبر، بدل أن يعود إلى صفحة الدخول.

**السبب (مقيسٌ لا مُخمَّن):** `env.logout_redirect` يقرّر الوجهةَ من **نوع البوّابة** وحدَه: مضيفُ المدينة ليس `*.asmsot.com` ⇐ بوّابةٌ `basic` ⇐ يحوّل إلى `/repo?bye=1` على افتراضِ أنّ **nginx** هو من يصادق. لكنّ المدينة صارت تصادق **داخل المنصّة** بدخول AD (`KAMC_AD_LOGIN`) — فالنموذجُ البيئيُّ تخلّف عن الواقع، والتحويلُ يقع في فراغ.

**الإصلاح:** حين يكون دخولُ AD مفعّلًا فالخروجُ ينتهي عند **صفحةِ دخولنا** أوّلًا وقبل أيّ منطقِ بوّابة: `/login?bye=1`. وسلوكُ Cloudflare والـbasic **لم يتغيّر** حين يكون AD مطفأً.

**وداعٌ صريح:** صفحةُ الدخول تعرض «✅ سُجّل خروجُك — أغلقتِ الجلسةُ وحُذفت كوكيزها» بدل الصمت.

**فُحص بالتشغيل** على نسخةٍ معزولةٍ (منفذ 5099، قاعدةٌ منسوخة، `KAMC_AD_LOGIN=1`، مضيف `ea.kamc.med.sa`):
`/logout` → **302 إلى `/login?bye=1`** وكوكيزُ `kamcsess` و`kamcad` تُمحى · اتّباعُ التحويل → **200** وصفحةُ الدخول ورسالةُ الوداع · و`/repo` بلا كوكي → **302 إلى `/login`** (الجلسةُ انتهت فعلًا).

## ② 🔑 صفحةُ الدخول الجديدة (كانت في 24-1)

شاشةٌ مقسومة: طبقاتُ المعمار الخمسُ حيّةً على `<canvas>` بنبضٍ ينزل بينها، ونصفٌ نظيفٌ للنموذج بحقوله كما هي (`ident` + `password` → `POST /login`). **آليّةُ المصادقة لم تُمسّ.** الخطُّ والشعارُ من `brand.py` مضمّنَين — صفر طلباتٍ خارجيّة (مُقاسٌ بالمتصفّح).

## ③ 🔢 ختمُ النسخة (كان في 24-2)

ختمُ 24-1 حُرِّر يدويًّا فبقيت بصماتُ ثلاثة ملفّاتٍ على ختم 19-1 والرقمُ **كهرمانيّ ⚠️**. هذه مختومةٌ بـ`build.freeze` فالبيانُ يطابق القرص.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.24-3.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.24-3.tar.gz | cut -c1-24    # يجب 5bf88396af4d8684fd36fa1a
./patch.sh patch-2026.08.24-3.tar.gz
```
وللتراجع: `./patch.sh --rollback`

## بعد التطبيق — ثلاثةُ تحقّقات

1. **الخروج**: اضغط زرَّ الخروج — يجب أن تصل **صفحةَ الدخول** مع رسالة «سُجّل خروجُك»، ثمّ جرّب `الرجوع` في المتصفّح: يجب ألّا تعود للوحة.
2. **رقمُ الإصدار**: **أخضر ✓** ويقرأ «إصدار 3 · 24/8». إن بقي كهرمانيًّا فأرسل قائمة الملفّات من `/repo/diag`.
3. **الدخول**: ادخلْ برقمٍ وظيفيٍّ صحيح — يعمل كما كان؛ وبخاطئٍ تظهر رسالةُ الخطأ داخل الصفحة الجديدة.

⚠️ **حدٌّ يُذكر بصدق**: إن كان **nginx** على المدينة ما زال يعترض `/logout` بـ401، فالطلبُ لا يصل المنصّةَ أصلًا ولن يعمل الإصلاح — عندها العلاجُ في إعداد nginx لا في الكود، وأبلغْني بما يظهر بالضبط.
