# نشر 24-5 — patch-2026.08.24-5

> **تجُبّ كل ما قبلها من رقع اليوم** (24-1 · 24-2 · 24-3 · 24-4) — طبّقْ هذه وحدها.
> بصمة الرقعة **داخل** التعمية: `32b3b7881430caf93992ca3b`.
> اثنا عشر ملفا · **لا تمس البيانات** — والجداول الجديدة تنشأ وحدها.

## ① 🪟 جرد ويندوز — داخل المنصة بلا وسيط

**أمر المالك:** «أتمتة الموضوع ليصبح عن طريق المنصة كاملة **ولا يمر بواسطة كلود** كسحب واستعراض للبيانات — لضمان عدم التسرب. اسمح بالتحديث التلقائي للحقول التقنية، ولا تعترض البيانات بأي شكل».

- **شاشة `/repo/win`** وتبويب في مصادر الربط: زر واحد يسحب من الخوادم ويطبق. **البيانات تمر من الخادم إلى قاعدة المستودع مباشرة** — لا تعبر أي جلسة محادثة ولا أي طرف ثالث.
- **التحديث التلقائي للحقول التقنية**: ستة عشر حقلا يملكها القارئ (نظام التشغيل وإصداره · المعالجات · الذاكرة · المساحة · عناوين IP · **البوابة وخوادم DNS والنطاق** · آخر إقلاع · افتراضي/مادي · المضيف). ⚠️ **وحقول الحوكمة لا تمس**: المالك والحرجية وتصنيف البيانات والنسخ الاحتياطي ونهاية الدعم — **قراءة لا تدهس قرارا**.
- **ثلاثة أوضاع معلنة في الشاشة**: `موقوف` · `تحديث تلقائي` (الافتراضي) · `تحديث وإنشاء`. والخادم **الجديد** يبقى في التحضير ينتظر اعتماد المعمار **ما لم يفعَّل الوضع الكامل صراحة** — فإنشاء عنصر قرار حوكمة لا نتيجة قراءة.
- **كل تغيير آلي يقيَّد في سجل التاريخ** بحقله وقيمته قبل وبعد (`win:الذاكرة` → `32 GB ← 128 GB`).
- **ويندوز صار مصدرا رابعا في مؤقت الفجر** — فالتحديث يجري يوميا بلا ضغطة زر.
- القراءة **قراءة فقط** عبر WinRM/PowerShell (`Get-*` فقط): لا كتابة على الخوادم ولا مسح شبكة ولا قراءة محتوى — ميتاداتا جرد فقط.

**الإعداد على المدينة** (في وحدة الخدمة أو البيئة):
`KAMC_WIN_HOSTS=10.11.72.71[,…]` · `KAMC_WIN_USER` · `KAMC_WIN_PASS` · اختياريا `KAMC_WIN_TRANSPORT=ntlm` · `KAMC_WIN_SSL=1` · وتلزم مكتبة `pywinrm`.

## ② 🏛 توظيف تقييم نضج البنية المؤسسية

شاشة `/repo/eamm` صارت تعمل لا تعرض فقط:
- **عداد حي من المستودع تحت اثني عشر سؤالا** يشير إلى الشاشة التي فيها دليله (وحدات البناء · المكتبة المرجعية · مصفوفات الربط · الاعتمادات · الاستثناءات · أداة البنية · التاريخ). ⚠️ **والعداد يقول أين الدليل لا أنه كاف** — كفايته حكم المقيم.
- **شريط تقدم لكل محور** يقيس المستويات المحصلة من سقفها.

## الفحص قبل الشحن (بالتشغيل)

نسخة معزولة بقاعدة منسوخة: دورة ويندوز كاملة — تحضير ← وضع «تحديث» يترك الجديد منتظرا (waiting 1) ← وضع «كامل» ينشئ (added 1، ١٣ حقلا، المصدر Windows) ← إعادة بلا تغيير (unchanged 1) ← تغيير الذاكرة يحدّث حقلا واحدا ويقيّده في التاريخ. والشاشتان تفتحان 200 وفيهما ٢٩ سؤالا و١٢ عدادا و٤ أشرطة. **وفحص الدخان: ٧ سليم · ٠ مختل.**

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.24-5.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.24-5.tar.gz | cut -c1-24    # يجب 32b3b7881430caf93992ca3b
./patch.sh patch-2026.08.24-5.tar.gz
```
وللتراجع: `./patch.sh --rollback`

## بعد التطبيق — خمسة تحقّقات

1. **`/repo/win`** تفتح، وتقول «غير مهيأ» حتى تضبط متغيرات الاتصال.
2. بعد الضبط: **«اسحب الآن وطبّق»** يعيد سطرا فيه: قُرئ · حُدّث (كم حقلا) · أُنشئ · بلا تغيير · ينتظر اعتمادا.
3. **`/repo/history`**: تظهر أسطر `win:` بقيمها قبل وبعد.
4. **`/repo/eamm`**: عدادات حية تحت الأسئلة وأشرطة تقدم للمحاور.
5. **رقم الإصدار أخضر ✓ «إصدار 5 · 24/8»**.

⚠️ **حدّ يُذكر بصدق**: الدورة فُحصت بالمحاكاة (`KAMC_WIN_MOCK`) لا على خادم ويندوز حقيقي — فأول سحب حقيقي على المدينة هو الاختبار الفعلي لطبقة WinRM. وإن رُدّ الاتصال فالسبب في الغالب صلاحية الحساب أو منفذ 5985/5986، لا في المنطق.
