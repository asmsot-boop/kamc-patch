# نشر 25-2 — patch-2026.08.25-2 · **مدمجة**

> **تُغني عن 24-16 وتشملها** — فلا تُطبَّق الاثنتان، هذه وحدها.
> **تُطبَّق فوق 24-14.** عشرة ملفّات: `wsman.py` و`eamm_req.py` جديدان ·
> `win_inventory.py` · `win_web.py` · `eamm.py` · `errors.py` · `app.py` ·
> `smoke.py` · `BUILD.json` · `HANDOVER.md`.
> بصمة الرقعة **داخل** التعمية: `d8ff48f04bc475fde682a582`.

## 🪟 ① حلّ جرد ويندوز — بابان على منفذ واحد

**بلاغ المالك 25/8 بالأدلّة:** كلود جهازه سحب من `10.11.72.71` اسم
الخادم ونظامه وإقلاعه بنجاح، **والمنصّة تردّ AccessDenied على الخادم
نفسه بالحساب نفسه**.

**والسبب أنّ البابين مختلفان:**

| الباب | ما يحتاجه على الخادم | ما يجلبه |
|---|---|---|
| **تعداد WMI** | قراءة `root/cimv2` فقط | كلّ شيء عدا البرامج المثبّتة |
| **قوقعة PowerShell** | عضوية **Remote Management Users** | كلّ شيء ومعه البرامج |

المنصّة كانت تنادي `run_ps` — **وهو يفتح قوقعةً بعيدة** فرُدّ. وكلود
جهازه عدّد WMI فمرّ. ⚠️ **والمصادقة لم تكن هي المشكلة قطّ**: هي تنجح،
والمرفوض **الإذنُ بفتح القوقعة** لا الدخول.

**ما تفعله الرقعة:** وحدة `wsman.py` تعدّ WMI (`Enumerate` ثمّ `Pull`)
**بمصادقة pywinrm نفسها ولا مكتبة جديدة**، و`_read_host` يجرّب
**التعداد أوّلا** لأنّه أقلّ صلاحيةً ثمّ يرجع للقوقعة إن سقط.

⚠️ **وبعض الخوادم لا تعطي العناصر إلّا عبر `Pull`** — وهو ما وقع على
خادم المدينة فعلا، والحالتان مقروءتان.

🔒 **قراءةٌ بنيةً لا وعدًا**: ثلاثة أفعال فقط — `Enumerate` · `Pull` ·
`Release`. ولا `Invoke` ولا `Put` ولا `Create`. ⚠️ **و`Win32_Product`
محظورة صراحةً**: تعدادُها **يُعيد تهيئة كلّ حزمة MSI على الخادم**،
فهي قراءةٌ بالاسم كتابةٌ بالأثر.

**وما تعذّر يُقال**: كلّ صفٍّ يُرفض يُقيَّد ويظهر في رسالة السحب
ويُحفظ مع الصفّ — **فجردٌ ناقصٌ يُعلن نقصه خيرٌ من جردٍ يبدو كاملا**.
والمتوقّع نقصُه بهذه القناة: **البرامج المثبّتة** (مصدرها سجلّ النظام
فتلزمها القوقعة)، وربّما **المنافذ المنصتة** إن لم يكن
`root/standardcimv2` مفعّلا.

**مقيس بالتشغيل** على خادم WS-Man مصنوع: ستّ حالات ردّ (عناصر مع
التعداد · عناصر بالسحب وحده · حقل متكرّر · حقل فارغ · خطأ داخل مظروف ·
تعداد بلا نهاية يُقطع ويُبلَّغ) — ثمّ الدورة كاملة من المظروف إلى صفّ
المستودع: الاسم والنطاق والمعرّف والذاكرة والأقراص والبوابة وخوادم
DNS وآخر إقلاع.

## 🧭 ② محرّك متطلبات النضج — شاشة ثالثة على `/repo/eamm`

كلّ مستوى في أسئلة الهيئة فُكِّك **متطلبات**، ولكلّ متطلب **مجسّ**
يستعلم قاعدة المنصّة ويعيد نعم أو لا **ومعه ما وجده نصّا**. فما تثبته
المنصّة يظهر ✅ بدليله، وما ينقص يظهر ⛔ ومعه **رابط الشاشة التي
يُعالَج منها** و**سؤال المقيّم التالي**.

⚠️ **ولا نموذج لغة في هذا المسار إطلاقا** — اللغويّ يخمّن ويجمّل،
**والإقرار الرقابيّ لا يحتمل تخمينا**. ⚠️ **ولا تخرج بيانة واحدة**:
المجسّات داخل عملية المنصّة على قاعدتها.

⚠️ **والمحرّك يقترح ولا يقرّ**: **الضغط على «اعتمده في التقييم» هو ما
يكتب المستوى** — فلا يُملأ إقرار أمام جهة رقابية إلا بيد صاحبه.

## 🔒 ③ وفحص أخطاء مؤجَّل (errors.py)

بند **DB-05** من تدقيق كودكس 22/8: فهرس فريد على بصمة الخطأ، **وقبله
دمج أيّ توائم قائمة** — وقد تكون وقعت على المدينة. **مقيس بالتشغيل**:
ثلاثة صفوف ببصمة واحدة (٣+٤+١ ضربة) دُمجت في صفّ بثماني ضربات،
**والمجموع ١٠ قبل وبعد فلم تضع ضربة**، والتشغيل الثاني بلا أثر.

## بعد التطبيق

**① الجرد**: `/repo/win` ← «اسحب الآن وطبّق». المتوقّع أن يُقرأ الخادم
عبر **تعداد WMI** وتظهر رسالة النجاح ومعها ما لم يُقرأ. وإن بقي
`AccessDenied` فقد أُغلق البابان معا، **والأقلّ كلفةً** منح الحساب على
`root/cimv2` صلاحيتَي **Enable Account** و**Remote Enable** — تكفيان
للتعداد وحده، وعضويةُ Remote Management Users تلزم للبرامج فقط.

**② النضج**: `/repo/eamm?view=req` — ثمانية عشر سؤالا بمتطلباتها.
⚠️ **والأرقام تختلف عن بيئة التطوير** فالمجسّ يقرأ قاعدة المدينة.

⚠️ **وفي وضع الإنجليزية** تظهر نصوص الشاشتين عربيّةً غير مترجمة حتى
تمرّ على حصاد الترجمة في `/repo/i18n`.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.25-2.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.25-2.tar.gz | cut -c1-24    # يجب d8ff48f04bc475fde682a582
./patch.sh patch-2026.08.25-2.tar.gz
```

وللتراجع: `./patch.sh --rollback`
