# نشر 25-5 — patch-2026.08.25-5 · **مدمجة**

> **تُغني عن 25-3 و25-4 وتشملهما** — لا تُطبَّق معهما، هذه وحدها.
> **تُطبَّق فوق 25-2.** تسعة ملفّات: `usage_alert.py` جديد ·
> `usage_watch.py` · `dawn.py` · `win_inventory.py` · `win_web.py` ·
> `app.py` · `smoke.py` · `BUILD.json` · `HANDOVER.md`.
> بصمة الرقعة **داخل** التعمية: `3aae603361376aae1bb1628d`.

## 🔔 ① قناة تنبيهات الامتلاء

طلب المالك (25/8): *«أحتاج إنشاء قناة تنبيهات للمعنيّين، وكذلك
إمكانية الاستثناء من الموارد للتنبيهات»* — والبريد قناةً وحيدة.

قسمٌ جديد **أسفل شاشة `/repo/usage`** لا شاشة منفصلة، فالإنذار يُقرأ
حيث يُرى المورد. فيه: **المعنيّون** (بريد · اسم · مستوى: الحرج فقط أم
الحرج والتحذير) · **الاستثناءات** · **ما أُسكت في هذه اللقطة** ·
**سجلّ الإرسال**.

### 📅 والإرسال من المسح الصباحيّ وحده

⚠️ **ولا يُرسَل من فتح شاشةٍ ولا من زرّ** — فقناةٌ تُطلق من مواضع
شتّى تُغرق صاحبها ثمّ **يحجبها فتسكت وهي تعمل**. المخرج الوحيد
`dawn.run()`، ويُذكر في ملخّص المسح، **وفشلُه لا يُسقط المسح**.

**وعند تغيّر مجموعة الإنذارات لا كلّ صباح** — ⚠️ **فرسالةٌ يوميّةٌ
بنفس المحتوى تُهمَل كلُّها**. والبصمة **بالأسماء لا بالنسب**: لو
دخلت النسبة لتغيّرت كلّ فجر (٩١٪ ثمّ ٩٢٪) فصار التنبيه ضجيجا.
ويُعاد التذكير بحرجٍ قائم بعد ٢٤ ساعة.

### 📨 وبريدٌ تعريفيّ لكلّ مضاف

يصله فور إضافته: ما يصله · ومتى · وأنّ المستثنى لا يُنذَر عنه ويبقى
مرئيّا · وبمن يتّصل إن لم يكن من شأنه. ⚠️ **فمن وجد إنذارا بلا سابق
علمٍ ظنّه بريدا مضلَّلا فحجبه.**

⚠️ **ولا يُسقط الإضافة إن تعذّر**: الاشتراك يُحفظ والبريدُ يُقال أنّه
لم يصل — **فربطُ عضويّةٍ بنجاح خادم بريدٍ يفقدهما معا**.

### 🔇 والاستثناء يُسكت الإنذار ولا يخفي المورد

⚠️ **والفرق بينهما هو كلّ شيء**: موردٌ يُحذف **يُنسى فيمتلئ صامتا**،
وموردٌ يُسكت **يبقى في الجدول موسوما بسبب إسكاته**. وهي سنّة `CVM`
القائمة عندكم. **ولكلّ استثناء سببٌ إلزاميّ**.

**واختيارٌ متعدّد** من موارد اللقطة بمربّعات و«تحديد الكلّ» و**سببٍ
واحدٍ يجمعها** — ⚠️ **فمن طُولب بكتابة السبب عشرين مرّة كتب «-»
عشرين مرّة، فصار الحقلُ الإلزاميّ زينة**.

⚠️ **والمختار من اللقطة يُطابَق تامّا**، والمكتوب باليد احتواءً:
**فاسمٌ يُطابَق احتواءً قد يُسكت جارَه** — «DS-01» تُسكت «DS-011»
بلا قصد، **وذاك صمتٌ لا يعلم به أحد**.

### 🔒 وآمنٌ معطّلا

لا مشترك ⇒ لا إرسال. ولا بريد مهيّأ ⇒ يُقيَّد **ويُقال في الشاشة
بشريط ظاهر**، ⚠️ **ولا يُترك فراغٌ يُظنّ سلامة**. **والقناة تصل
المدينة فارغةً** — لا مشترك ولا استثناء حتى تُضيفهم.

**مقيس بالتشغيل**: الإضافة أرسلت التعريف ويذكر المسح ومستوى المشترك ·
وببريدٍ معطّل حُفظ الاشتراك وقيل السبب · وأوّل مسح أرسل لاثنين
(تحذيرٌ وحده فلم يُوقَظ مشترك «الحرج فقط») · **ومسحٌ ثانٍ بلا تغيير
لم يُرسل** · وبعد رفع استثناءٍ ظهر حرجٌ فأُرسل للأربعة · واستثناءُ
ثلاثةٍ دفعةً واحدة **وبقاؤها كلُّها ظاهرةً في جدول الامتلاء**.

## 🗺 ② خريطة صلاحيات جرد ويندوز (من 25-4)

قياسُ الشلّ على `KAMCWEBEXT01`: **`KAMC\EA` عضو
`__WinRMRemoteWMIUsers__` ولا عضوَ في `Remote Management Users`** —
⚠️ **وهذا وحده تفسيرُ أنّ التعداد يمرّ والقوقعة تُردّ**.

| النطاق | الحال | ما يعنيه للجرد |
|---|---|---|
| `root/cimv2` | ✅ مسموح | **جوهرُ الجرد كلُّه** |
| `root/cimv2 → Win32_Service` | ⛔ محجوب | الخدمات |
| `root/StandardCimv2` | ⛔ محجوب | المنافذ المنصتة |
| `root/default` | ⛔ محجوب | البرامج المثبّتة |
| `root/webadministration` | ⛔ محجوب | مواقع IIS |

**فالجرد يعمل بلا منحٍ جديد**، والمحجوب يُعلَن **ومعه منحُه المحدَّد**.

⚠️ **وعيبٌ في شفرتنا أُصلح**: `_run_wmi` كان يسجّل اسمَ صنف الاستثناء
لا نصَّ الخادم، **فسببُ سقوط التعداد ضاع وعُرض خطأ القوقعة وحده**.
الآن: نصُّ الخادم كما ردّه · البابان يُعلنان معا · **والهويّة والوجهة
في نصّ الخطأ** · والشرحُ لا يمحو الدليل. وزرّ **🔬 افحص القناة**
يجرّب اثنتي عشرة توليفةً ويعرض نصَّ الخادم كما ردّه.

⚠️ **وتنبيه**: إن كان الجردُ اليدويّ سحب «البرامج المثبّتة» من
`root/cimv2` **فالغالب أنّه استعمل `Win32_Product`** — **وتعدادُه
يُعيد تهيئة كلّ حزمة MSI على الخادم**. وهو محظورٌ بنيويا عندنا،
والبديل الآمن `Win32_InstalledWin32Program`.

## بعد التطبيق

**①** `/repo/usage` — القسم أسفل الجدول. أضف المعنيّين، وجرّب
«اختر من إنذارات هذه اللقطة» لاستثناء عدّة موارد بسببٍ واحد.
⚠️ **ولن يُرسل شيء حتى يُهيّأ SMTP** — والشريط الأصفر يقولها.

**②** `/repo/win` — «اسحب الآن وطبّق».

**③** بوّابة الدخان صارت **٤٢ شاشة**، وصفر انحراف في ١٦١ ملفا.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.25-5.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.25-5.tar.gz | cut -c1-24    # يجب 3aae603361376aae1bb1628d
./patch.sh patch-2026.08.25-5.tar.gz
```

وللتراجع: `./patch.sh --rollback`
