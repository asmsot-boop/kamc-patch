# نشر 25-3 — patch-2026.08.25-3

> **تطبَّق فوق 25-2.** ستّة ملفّات: `win_inventory.py` · `win_web.py` ·
> `app.py` · `smoke.py` · `BUILD.json` · `HANDOVER.md`.
> بصمة الرقعة **داخل** التعمية: `91cdd0bc0e080a047121c9d8`.

## 🔴 عيبٌ في شفرتنا: نصُّ الخادم كان يُبتلع

بعد تطبيق 25-2 ظهرت رسالة «رُفض البابان معا». **وهي لم تكن دليلا بل
تخمينا**: `_run_wmi` كان يسجّل **اسمَ صنف الاستثناء** (`WsmanFault`)
لا **نصَّ الخادم**، فسببُ سقوط التعداد ضاع، والرسالةُ المعروضة كانت
من محاولة القوقعة وحدها.

⚠️ **فمن رأى «AccessDenied» ظنّ أنّ التعداد لم يُجرَّب أصلا** — وهو
جُرِّب ورُدّ بنصٍّ لم نره.

**ما تصلحه الرقعة:**

- **نصُّ الخادم يُحفظ** كما ردّه، لا اسمُ صنف الاستثناء.
- **البابان يُعلنان معا**: `التعداد: … ⟵⟶ القوقعة: …`.
- **الهويّة والوجهة في نصّ الخطأ**: `[حساب@خادم:منفذ نقل/مصادقة]` —
  فسؤال «بأيّ حساب اتّصلت؟» يُجاب من الشاشة لا من قراءة الشفرة.
- **والشرحُ لم يعد يمحو الدليل**: الجملة المفهومة تبقى، **ويُلحق بها
  النصّ الخام** بين قوسين.

## 🔬 وزرّ «افحص القناة»

⚠️ **ولا يُشخَّص عطبُ قناةٍ بالتأمّل.** جهازُك يقرأ الخادمَ والمنصّةُ
تُردّ عنه **بالحساب نفسه** — والفرقُ يُقاس لا يُستنتج.

الزرّ يجرّب **(منفذ × تشفير × مصادقة) × (تعداد · قوقعة)**: منفذك من
الخزنة و5985 و5986، بمصادقة `ntlm` و`kerberos` و`basic`، على البابين
— **اثنتا عشرة توليفةً على ستّ قنوات** — ويعرض جدولا فيه لكلّ خليّة
**نصُّ الخادم كما ردّه**. ⚠️ **ولا يُجمَّل الرادّ ولا يُخفى.**

**مقيس بالتشغيل**: اثنتا عشرة خليّةً جُرِّبت وعُرضت، وحين لا خادم في
الخزنة تظهر العلّة نصّا لا صفحةً فارغة.

## بعد التطبيق

افتح `/repo/win` واضغط **🔬 افحص القناة**. والمطلوب منك بعدها **سطر
واحد**: أيّ صفٍّ ظهر ✅ — أو نصُّ الردّ في صفّ `5985 / HTTP / ntlm /
تعداد WMI`. ومنه يُعرف الفرقُ بين جهازك والمنصّة **بيقين لا ترجيح**.

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.25-3.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.25-3.tar.gz | cut -c1-24    # يجب 91cdd0bc0e080a047121c9d8
./patch.sh patch-2026.08.25-3.tar.gz
```

وللتراجع: `./patch.sh --rollback`
