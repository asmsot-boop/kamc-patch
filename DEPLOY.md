# نشر 25-4 — patch-2026.08.25-4

> **تُغني عن 25-3 وتشملها** — لا تُطبَّق الاثنتان، هذه وحدها.
> **تُطبَّق فوق 25-2.** ستّة ملفّات: `win_inventory.py` · `win_web.py` ·
> `app.py` · `smoke.py` · `BUILD.json` · `HANDOVER.md`.
> بصمة الرقعة **داخل** التعمية: `efd2e1374bbbd3b1d1feee8a`.

## 🗺 الفحص من الشلّ حسم الأمر — والبنية صحيحة

قياسُ 25/8 على `KAMCWEBEXT01` بحساب `KAMC\EA`: **الحساب عضو
`__WinRMRemoteWMIUsers__` ولا عضوَ في `Remote Management Users`**.
⚠️ **وهذا وحده تفسيرُ أنّ التعداد يمرّ والقوقعة تُردّ** — وهو ما
بُنيت عليه 25-2 قبل أن يصل القياس.

| النطاق | الحال | ما يعنيه للجرد |
|---|---|---|
| `root/cimv2` | ✅ مسموح | **جوهرُ الجرد كلُّه** |
| `root/cimv2 → Win32_Service` | ⛔ محجوب | الخدمات |
| `root/StandardCimv2` | ⛔ محجوب | المنافذ المنصتة |
| `root/default` | ⛔ محجوب | البرامج المثبّتة |
| `root/webadministration` | ⛔ محجوب | مواقع IIS |

**فالجرد يعمل اليوم بلا أيّ منحٍ جديد**، والمحجوب يُعلَن **ومعه منحُه
المحدَّد** — ⚠️ **فالعبارةُ العامّة «صلاحيات ناقصة» لا يُفعل بها شيء**.

## 🔴 وعيبٌ في شفرتنا كان يخفي الدليل

`_run_wmi` كان يسجّل **اسمَ صنف الاستثناء** (`WsmanFault`) لا **نصَّ
الخادم** — فسببُ سقوط التعداد ضاع، والمعروضُ خطأُ القوقعة وحدها.
⚠️ **فمن رأى «AccessDenied» ظنّ أنّ التعداد لم يُجرَّب.**

**المُصلَح:** نصُّ الخادم يُحفظ كما ردّه · البابان يُعلنان معا ·
**الهويّة والوجهة في نصّ الخطأ** `[حساب@خادم:منفذ نقل/مصادقة]` ·
**والشرحُ لم يعد يمحو الدليل الخام**.

## ⚠️ وتنبيهٌ يخصّ البرامج المثبّتة

إن كان الجردُ اليدويّ سحب «البرامج المثبّتة» من `root/cimv2` **فالغالب
أنّه استعمل `Win32_Product`** — ⚠️ **وتعدادُ هذا الصفّ يُعيد تهيئة كلّ
حزمة MSI على الخادم**، فهو قراءةٌ بالاسم كتابةٌ بالأثر، **وقد يُعيد
تشغيل خدماتٍ على خادمٍ إنتاجيّ**.

**وهو محظورٌ بنيويا في `wsman.py`** — والمنصّة تجرّب البديل الآمن
`Win32_InstalledWin32Program`، فإن حُجب **قالت ولم تلتفّ عليه**.

## 🔬 وزرّ «افحص القناة»

يجرّب **(منفذ × تشفير × مصادقة) × (تعداد · قوقعة)** — اثنتي عشرة
توليفةً على ستّ قنوات — ويعرض **نصَّ الخادم كما ردّه**. يبقى للخوادم
القادمة التي تختلف تهيئتها.

## بعد التطبيق

`/repo/win` ← **اسحب الآن وطبّق**. المتوقّع: **يُقرأ الخادم بنجاح**
عبر تعداد WMI، ويصل المستودعَ اسمُ المضيف والنطاق والنظام والذاكرة
والمساحة وعنوان IP والبوابة وآخر إقلاع وافتراضيّ/ماديّ — **ومعها
سطرٌ يقول ما لم يُقرأ وما يفتحه**.

**مقيس بالتشغيل** على خادم يحاكي خريطة الصلاحيات نفسها: الجرد نجح،
والثلاثة المحجوبة ظهرت بمنحها، وعشرُ سماتٍ وصلت المستودع.

**⚠️ ولا خطر على بياناتك**: `_merge` يتخطّى الفارغ، والجردُ التفصيليّ
يُنزع، والإدراجُ يُرشَّح بـ`READER_KEYS` — **فصفٌّ محجوبٌ لا يمحو
بيانا أغنى**. (فُحص مسارُ الكتابة كلُّه قبل الشحن.)

## التطبيق

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.25-4.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
sha256sum patch-2026.08.25-4.tar.gz | cut -c1-24    # يجب efd2e1374bbbd3b1d1feee8a
./patch.sh patch-2026.08.25-4.tar.gz
```

وللتراجع: `./patch.sh --rollback`
