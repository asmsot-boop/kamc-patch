# نشر 18-1 — patch-2026.08.18-1

> **تجُبّ كل ما قبلها** (لقطة جذر كاملة · 170 ملفا · النسخة 2026.08.18-1) — تصلح من أي نسخة سابقة.
> بصمة الرقعة داخل التعمية: `d7642fe90f92fafd5592b5d8` — قارنها بعد الفك قبل التطبيق.
> `shared_viz.py.enc` لم يتغير — **لا خطوة يدوية**.

## ما فيها — توحيد مسارات إنشاء المستخدم (بلاغ المالك 18/8: «مستخدم جديد بلا بريد ترحيب ولا أثر في السجلين»)

1. **🔀 مسار كتابة واحد في `people`**: شاشة «المستخدمون والأدوار» (`/repo/perms` — إضافة وتغيير الدور) وشاشة المدراء (منح قارئ) كانت تكتب في الجدول خامًا فتتجاوز `roles.grant()` — **فيسقط بريد الترحيب وفحص تكرار الرقم الوظيفي معًا بلا أثر في أي سجل**. المواضع الثلاثة صارت تمر عبر `grant()` وترث فحوصه وقيده.
2. **📧 الترحيب عند الإضافة الأولى وحدها** (لا عند تغيير الدور)، ونتيجته تصارح في شريط الرسالة («أرسل الترحيب» أو «تعذر: السبب») وتقيد في `/repo/outbox` نجحت أم فشلت.
3. **🪪 دور «reader» يهاجر إلى «مطلع»**: شاشة المدراء كانت تزرع دورًا غير معرف في سلم الأدوار أصلًا (يعمل قراءة بالمصادفة) — الجديد يمنح «مطلع»، وأي صف قديم يحمل «reader» يصحح تلقائيًّا عند أول تحديث له من شاشة الإضافة.
4. **🌐 سطر سجل الإصدارات في `/repo/docs` يترجم**: «الإصدار · أول إصدار · آخر تغيير» كانت تبقى عربية في العرض الإنجليزي (عقدة مركبة بتاريخ متغير لا يلتقطها القاموس) — صارت بالزوج في المصدر.
5. حارس انحدار جديد ع٧٨ (بتنظيف أثره من history/trail) — فحص مستقل (سونيت) على الدفعة أغلقت ملاحظتاه. **المنظومة 1027/0**.

## التطبيق (المعتاد)

```bash
cd ~/kamc_ea
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc -o latest.tar.gz.enc
curl -sL https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256 -o latest.sha256
sha256sum -c <(echo "$(cat latest.sha256)  latest.tar.gz.enc")   # يجب OK
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch-2026.08.18-1.tar.gz -pass file:$HOME/.config/kamc/deploy_pass
./patch.sh patch-2026.08.18-1.tar.gz
```

## بعد التطبيق — تحققان

1. من `/repo/perms` أضف مستخدمًا تجريبيًّا (أو أعد إضافة موجود): شريط الرسالة يجب أن يذكر الترحيب صراحة عند الإضافة الأولى («أرسل الترحيب» أو «تعذر: السبب»)، و`/repo/outbox` يقيد صفًّا بالنتيجة نفسها. **صمت الشريط عن الترحيب في إضافة أولى = الرقعة لم تطبق.**
2. الوضع الإنجليزي على `/repo/docs`: سطر سجل الإصدارات كله إنجليزي (Version · First issued · Last changed).
