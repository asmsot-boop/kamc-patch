# رقعة 2026.08.15-7 — **بوابة العربي في الإنجليزية**

> 🔐 `-pass file:$HOME/.config/kamc/deploy_pass` · ⛔ **بلا `sudo`**.

المالك 15/8: *«ما زالت النصوص العربية تظهر في النسخة الإنجليزية — تعبنا
من هذه النقطة.»*

```
١٠٥ لفظة عربية في العرض الإنجليزي  →  صفر غير مبرر
                                      ٣٨ مسموحة بسبب مكتوب
```

---

## 🩸 ولماذا تعب — ثلاث طبقات كانت تطمئنه وهو يرى العطب بعينه

**① الحارس** يقيس نص الواجهة **القصير** وحده، فالجمل الطويلة تمر تحته.

**② والمقياس** كان يقيس **عينة مقصوصة عند ٣٤ حرفا** ثم يسأل «أهي ≤٤٢؟»
— **فالشرط يتحقق دائما مهما طال الأصل**. فكان نص المعيار الرقابي
(٤٠٠ حرف) وأسماء الإدارات تعد «نص واجهة عربيا متسربا»، **وكان تمييز
الواجهة من المحتوى لا يعمل قط**.

**③ والكاشف** يمسح ما في **القائمة الجانبية فقط** — **٣٣ شاشة لم تمسح
قط**، ومنها `/repo/perms` و`/repo/diag` اللتان يستعملهما يوميا. وكان
يقرأ `bundle.tar.gz` نصا فيخرج **٩٨٣ «لفظة عربية»** وهي بايتات مضغوطة،
**تغرق الرقم الحقيقي عشرة أضعاف**.

**وحارس يخضر والمالك يرى العطب أسوأ من غيابه** — يشتري الطمأنينة بثمن
الثقة.

## ✅ ما أصلح فعلا

- **٥٠ عبارة واجهة** في `review/*` و`search` · **٩ أسماء شاشات** في
  السجل والتدقيق.
- **`canvas`** صار يقرأ `name_en` **الموجود في العمود المجاور** —
  **وبيانة لها ترجمة لا تعرض أسوأ من بيانة بلا ترجمة**: الأولى إهمال
  والثانية نقص.
- **التاريخ الهجري وأسماء الأيام و«قبل ٣ ساعات»** تتبع لغة العرض.
  ⚠️ **والهجري بأسماء شهوره داخل صفحة إنجليزية نص عربي لا تاريخ.**
- **جمل الرقيب المولدة** صارت ثنائية — **والمولد أولى من المترجم بأن
  يعرف**، فالنص مركب من اسم عنصر ورقم **فلا مفتاح ثابت له في القاموس**.
- **«No Organisation Unit مسجل بهذا اNoسم»** — لاحظ «اNoسم»: الاستبدال
  بعد التوليد **كان يقطع الكلمة نفسها**. تولد الآن بلغة واحدة من أصلها.
- **وثلاثة مواضع قاومت البذر**: الوسم `<b>` يشطر النص عقدتين والقاموس
  يطابق العقدة كاملة — **فما شطره وسم لا يترجمه قاموس**. أصلحت في المصدر.

## 🇸🇦 والحارس الحاسم — `ar_allow.py`

**كل عربي في العرض الإنجليزي إما مسموح بسبب مكتوب أو أحمر.** والأصناف:

| الصنف | السبب |
|---|---|
| **نص المنظم** | معيار قياس وADR — **ترجمته تحريف**، ووثيقة تقول «البند يتطلب كذا» وهو لا يتطلبه تقدم لجهة رقابية |
| **سجل تاريخي** | رسائل الرقع وقيود الأخطاء — **كتبت يوم كتبت**، وترجمتها بأثر رجعي تزوير لطيف |
| **بيانة المستودع** | اسم بلا توأم إنجليزي — ⚠️ **وهذه ليست إعفاء بل دين**: تسقط يوم يملأ `name_en` |
| **مثال مقصود** | «الادارة» تجد «الإدارة» — **وترجمة المثال تمحو المثال** |

⚠️ **ولا استثناء بمسار**: «استثن `/repo/adr` فمحتواه عربي» تصير قاعدة
الغد **وتخفي عربي واجهة حقيقيا يظهر هناك لاحقا**. فالاستثناء **بالسبب
لا بالموضع**، ومكتوب حيث يقرؤه من يراجع.

**وأمسكت البوابة عطبين أثناء البناء**: مفتاح في القاموس كان **يحمل اسم
عنصر من المستودع** فيصير خاصا بعنصر واحد لا يطابق غيره أبدا، **ويحمل
تشكيل الاسم إلى القاموس فيسقط حارس التجريد**. ولما حذفته **حمرت البوابة
في الثانية وقالت أين**.

**الحزمة: ٨٥٩ نجح · صفر فشل.**

---

## التطبيق

```bash
cd ~/kamc_ea
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch.tar.gz \
  -pass file:$HOME/.config/kamc/deploy_pass
sha256sum -c <(echo "$(cat latest.sha256)  patch.tar.gz")
./patch.sh patch.tar.gz          # ⛔ بلا sudo
```

**وبعدها قس لا تصدق الرسالة**: `/repo/diag` يقول **2026.08.15-7** ·
**بدل اللغة إلى الإنجليزية وتجول في الشاشات** — وما تجده عربيا يجب أن
يكون من الأصناف الأربعة أعلاه. **وإن وجدت غيرها فالبوابة أخطأت، لا
أنت** — أرسله لي وأضيفه.

⚠️ **وبيانات المدينة أوسع من الورشة**: عناصر بلا `name_en` ستظهر عربية
ومصنفة «بيانة مستودع» — **وهي دين لا إعفاء**، تسقط يوم يملأ التوأم.
