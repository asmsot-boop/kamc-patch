# رقعة 2026.08.15-3

> 🔐 **كلمة التشفير دُوّرت 15/8 09:57** — الكلمة السابقة انكشفت في محادثة.
> **والكلمة الجديدة لا تكتب في أمر ولا محادثة أبدا** — تقرأ من ملفها:
> `-pass file:$HOME/.config/kamc/deploy_pass`
>
> ⚠️ **وخادم المدينة لا يملك الكلمة الجديدة بعد** — ضعها فيه أولا
> (بيدك، خارج القناة) وإلا فشل الفكّ بـ`bad decrypt`.

> ⛔ **ولا `sudo` مع `patch.sh`.** والسكربت الآن **يرفض** لا يحذّر:
> تحت `sudo` يصير المستخدم `root` فلا يجد وحدة `--user`، **فيستخرج في
> مسار آخر ويقول «تمّ» ولم يقع.** وصار يطبع مسار الاستخراج والمستخدم.

**ثلاثة أبواب**: تحرير وثائق المكتب · مدراء الإدارات · الفجوات بأنواعها.

---

## ① تحرير وثائق المكتب في المنصّة

المالك: *«بعض الملفات لا أستطيع تعديل محتواها، تنزل PDF»* — مثل **وثيقة
قرار تأسيس المكتب**. كانت تُولَّد من الشفرة ولا تُحرَّر، **ومسودّة قرار
تأسيس لا تُحرَّر ليست مسودّة**.

الآن: زرّ **تحرير** بجانب كل وثيقة مكتب، والنصّ المحرَّر يُخزَّن ويُقرأ
منه الـPDF نفسه — لا من المولّد. ووسم **«محرَّرة»** يميّزها.

بنيويًّا: مساحة أسماء `office:` تفصل مفاتيح المكتب عن مفاتيح الوثائق
العامّة — **و`charter` موجود في الاثنين**، فبلا فصلٍ لصدمَ أحدهما الآخر.

## ② مدراء الإدارات — `/repo/directors`

جدولٌ مستقلّ عن `people`: لكل إدارة **مدير واحد** باسمه وبريده ورقمه
الوظيفيّ. المطابقة **غير حسّاسة للتشكيل** (تستعمل `org_seed.norm`)،
فـ«التحتيّة» و«التحتية» إدارة واحدة.

- **لصق جماعيّ** من إكسل: سطر لكل إدارة، الفواصل tab أو `,` أو `؛` أو `|`.
  **والسطر السيّئ يُسمّى برقمه وسببه** — لا يُبتلع صامتًا.
- شريط تغطية: كم إدارة لها مدير وكم بلا، **وأسماء الناقصة** لا عددها.
- الحذف **يعطّل ولا يمحو** — فالتاريخ يبقى.

⚠️ **ولا يُخترع بريد**: إدارة بلا مدير مسجَّل تظهر ⚠️ ولا يُرسَل إليها شيء.

## ③ الفجوات بأنواعها الثلاثة — `/repo/gapwork`

**وثلاث فجوات لا واحدة**، ولكلٍّ مسار إغلاقٍ مختلف. ومن خلطها طارد فجوة
قرارٍ بتذكير موظّف — **وهو ما لا يغلقها أبدًا**، لأنّ الموظّف لا يملك القرار.

| | النوع | تُغلق بـ |
|---|---|---|
| 🔵 | **بيانات** | ملء حقلٍ في المستودع |
| 🟠 | **معمارية** | عملٍ (ترحيل · تجديد · تقاعد) — الواقع يخالف الهدف |
| 🔴 | **قرار** | قرار المالك — لا هدف معتمد أصلًا |

- **ثلاث بطاقات** بالعدد ومتوسّط العمر وكم فجوةً بلا مُرسَلٍ إليه.
- **الصفّ يتوسّع في مكانه** فيعرض **مكوّناته المتأثّرة بأسمائها**
  (اسم · نوع · مالك · حرجيّة)، **وبلا مالكٍ يظهر أحمر** — فمن رأى
  «١٢ خادمًا متأثّرًا» لم يعرف أيّها، فلا يُسند ولا يسائل.
- في الصفّ نفسه: **النوع · المسؤول · تاريخ الالتزام** تُحفظ مباشرة.
  والعمر يصفرّ فوق ٣٠ يومًا ويحمرّ فوق ٦٠.
- **📧 أرسل التقرير** — وقبله سطرٌ يقول **إلى مَن سيصل بالضبط**؛
  **والمفاجأة في البريد الصادر أسوأ من عدم إرساله**. وبلا مدراء
  مسجَّلين **يرفض الإرسال ويقول لماذا** — ولا يرسل إلى فراغ.
- **📊 صدِّر إكسل** — `/repo/gapwork.csv` ببصمة BOM للعربيّ، وفيه
  عمود «مكوّنات متأثّرة».
- ذيلٌ يقول موقعها: **TOGAF الطور E/F · قياس التخطيط التنفيذيّ**.

🔑 **وملء فجوة بيانات يفتح فجوة معمارية**: تكتب تاريخ نهاية الدعم فتكتشف
أنّه مضى. **فالمستودع الممتلئ قد يكون أسوأ خبرًا، لكنّه صادق.**

---

## الحراسة

- **ع٦٦** — وثائق المكتب تُحرَّر، والـPDF يقرأ المحرَّر لا المولّد.
- **ع٦٧** — الأنواع الثلاثة موصوفة · الافتراض **بيانات لا قرار**
  (ورفع فجوةٍ للمالك بلا داعٍ يستهلك رصيدًا لا يُعوَّض) · **كلّ مُرسَلٍ
  إليه مسجَّل في مدراء الإدارات** · الحقل الفارغ لا يمحو القديم ·
  **والفحص يُعيد ما غيَّر** — فهذا مستودع المالك الحيّ لا قاعدة اختبار.

**الحزمة**: ٥٧٧ نجح · ٠ فشل في الانحدار. ويبقى الأحمر الواحد المعروف:
تسرّب نصٍّ عربيٍّ في العرض الإنجليزيّ (١٢ يتجاوز الحدّ ٦) — **وأكثره
قِيَمُ بيانات لا نصُّ واجهة**، وهو في السجل بندًا مستقلًّا.

## التطبيق

```bash
cd ~/kamc_ea
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.tar.gz.enc
curl -fsSLO https://raw.githubusercontent.com/asmsot-boop/kamc-patch/main/latest.sha256
openssl enc -d -aes-256-cbc -pbkdf2 -in latest.tar.gz.enc -out patch.tar.gz \
  -pass file:$HOME/.config/kamc/deploy_pass
sha256sum -c <(echo "$(cat latest.sha256)  patch.tar.gz")
./patch.sh patch.tar.gz          # ⛔ بلا sudo
```

**وبعد التطبيق قِس، لا تصدّق الرسالة**: افتح `/repo/diag` وتأكّد أنّ
النسخة صارت **2026.08.15-3**، وأنّ `/repo/directors` و`/repo/gapwork.csv`
يستجيبان.
